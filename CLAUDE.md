# 지구자기장 측정 입지 선정 프로젝트 — Claude 작업 규칙

> 이 파일을 읽고 작업에 임한다. 새로운 변경이 생기면 즉시 이 파일을 업데이트한다.

---

## 1. 프로젝트 개요

대한민국 지구자기장 총자력 측정 최적 입지를 자동 선정하는 Python 시스템.
- 인공 자기 간섭 요소(송전탑·철도·도시·지질) 제외 후 다기준 점수 산정
- Folium 기반 대화형 HTML 지도 생성 → GitHub Pages 배포

---

## 2. 주요 스크립트 및 실행 순서

```
① python geomag_site_selection.py   # 분석 + 지도 생성 → output/ 저장
② python build_docs.py              # output/ → docs/ 변환 (GeoJSON 단순화)
③ python create_methodology_doc.py  # 방법론 Word 문서 생성 → docs/output/ 저장
```

| 스크립트 | 역할 | 출력 |
|---|---|---|
| `geomag_site_selection.py` | 메인 분석·지도 생성 | `output/` |
| `build_docs.py` | 배포 빌드 (GeoJSON 단순화) | `docs/` |
| `create_methodology_doc.py` | 방법론 Word 문서 생성 | `docs/output/` |

### LMM (지역 자기장 모델) 계산기 — 별도 파이프라인

```
① python lmm_build.py         # 모델 적합·검증 → docs/data/lmm_model.json
② python lmm_web.py           # 웹 계산기 → docs/lmm.html (단일 파일, 오프라인)
③ python lmm_excel.py         # 엑셀 계산기 → docs/output/*_LMM_계산기.xlsx
④ python create_lmm_report.py # 구축현황 보고서 → docs/output/*_LMM_구축현황보고서.docx
```

②③④ 는 모두 `lmm_build.py` 산출물을 소비하므로 **반드시 ① 먼저 실행**한다.
보고서는 수치를 하드코딩하지 않고 `lmm_model.json` 에서 읽으므로 재적합 시 자동 갱신된다.

보조 스크립트:
```
python lmm_external_multi.py [--mode plane|idw|nearest]  # 관측소 4소 공간보간 보정량
python lmm_sensitivity.py  # 측점 수 대비 자유도 민감도 (자문 요구 항목)
python lmm_fieldbook.py    # 야장 → 관측 일시 복원
python lmm_verify2019.py   # 2019 야장 신뢰도 검증 (원시 판독값 역산 감사)
python lmm_cyg.py --start YYYY-MM-DD --end YYYY-MM-DD   # CYG 1분 자료 + Kp 수집
python lmm_external.py     # 야장 일시 × CYG × Kp → 외부장 보정량
python create_lmm_slides.py  # 시범 구축(안) 발표자료 → docs/output/*_LMM_시범구축_발표자료.pptx
```

검증 진단 및 연구 기록:
```
① python lmm_diagnose.py                 # 일괄 진단 → docs/data/lmm_diagnosis.json
② python create_lmm_validation_report.py # 연구 기록 → docs/output/*_LMM_검증연구기록.docx
```

②는 ①의 JSON 만 읽으므로 **수치가 문서에 하드코딩되지 않는다.** 자료가 갱신되면
①→② 순서로 재실행하면 문서도 자동 갱신된다. ①이 수행하는 검정은 13절 참조.

자문회의 발표자료 (두 축 종합):
```
python create_advisory_slides.py   # → docs/output/*_지자기_자문회의_발표자료.pptx (22장)
```

**Style D — Bauhaus Geometry 학술 절제 변형** (Phlegonlabs/Powerpoint-fancy-design 참조).
흰 배경 + 4색(차콜 #1A1A1A · 네이비 #16324F · 코발트 #1D4ED8 · 오렌지 #E8531F),
도형은 원·삼각형·사각형만, 면마다 도형 하나는 화면 밖으로(bleed), 그라디언트·그림자·
라운드 금지. **제목은 짧은 결론형 문장**("약 1,800점이 103개소로 좁혀진다"),
산문 대신 도식 라벨. 연구 논리 자체를 도형으로 — 인과구조도·깔때기·4층 레이어·
데이터 파이프라인·로드맵+게이트·질의 의사결정 매트릭스(쟁점|현재 상태|자문 질문).

글꼴 치환(설치본): Bebas Neue→**Bahnschrift**(DIN 계열), 한글 제목 **Noto Sans KR
Black**, 본문 Noto Sans KR, 모노 라벨 **Consolas**(라틴·숫자 전용 — 한글은 ea 로
Noto Sans KR 물림). 수치는 하드코딩하지 않고 `candidates_p*.geojson` ·
`lmm_model.json` · `lmm_diagnosis.json` · `lmm_build.igrf_dif` 에서 읽는다.

| 유의점 | 내용 |
|---|---|
| **pptx.enum.line 없음** | 이 환경 python-pptx 에는 `MSO_LINE` 이 없다. 파선은 `a:prstDash` XML 을 직접 붙일 것 (`seg(dash="dash")`) |
| **COM Export 경로** | PowerPoint COM 의 `Slide.Export` 는 **백슬래시 경로만** 받는다. 슬래시(`C:/...`)면 COMException |
| **색 띠 위 라벨** | 색 채운 띠의 오른쪽 라벨은 띠 안에서 끝나게 잡는다. 띠 밖까지 걸치면 잘린 것처럼 보인다 |
| **부 표지 꼬리말** | 흘림 도형이 왼쪽 아래를 덮으므로 부 표지에는 쪽번호만 남긴다 |
| **도형 위 텍스트** | `geo()` 도형의 자체 text_frame 을 쓰면 중앙 정렬이 편하다 (번호 사각 등) |
| **모식도 정합** | 서브격자 도해의 「선정점」은 반드시 통과 격자 집합에서 골라야 한다 |
| **빈 bar 범례** | `ax.bar([], [], color=...)` 는 색이 안 물린다. `Patch` 핸들을 직접 넘길 것 |
| **그림자 제거** | `shadow.inherit=False` 만으로 부족 — `p:style` 의 테마 effectRef 까지 떼야 한다 |
| **검토 방법** | PowerPoint COM(`$s.Export(path,"PNG",1600,900)`)으로 렌더 확인. LibreOffice 보다 한글 재현이 정확하다 |

**Claude Design 디자인 시스템** (claude.ai/design, 프로젝트 `37131a82-f4ec-44a1-b3fd-70f318c603bb`
「지자기 프로젝트 디자인 시스템」) — 위 발표자료의 색·활자·판면·구성요소를 14개 카드로
정리해 두었다. Foundations(개요·색·활자·판면) / Components(머리글·숫자타일·카드·흐름·
괘선표·요지띠·질의) / Templates(표제·부 표지) / Charts(도판 규칙).

⚠️ **자동 연결이 아니다.** python-pptx 는 이 프로젝트를 읽지 못한다. 실제 값은
`create_advisory_slides.py` 상단 상수에 있고 디자인 시스템은 그 사본이므로,
**값을 바꾸면 두 곳을 함께 고칠 것**. 어긋나면 스크립트가 사실이다.
갱신은 `DesignSync`(list → finalize_plan → write_files) 로 한다 —
`finalize_plan` 은 `deletes` 를 비워도 **반드시 넘겨야** 한다.

`create_lmm_slides.py` 는 4-층 구조와 6단계 절차를 7장으로 재구성한 pptx 를 만든다.
**학술 조판**을 따른다 — 세리프 전면(라틴 Cambria / 한글 Noto Serif KR), 절 번호,
번호 붙인 식, 「표 n」「그림 n」 캡션, booktabs 가로 괘선 3줄.
라운드 카드·배지·그라디언트·다크 슬라이드는 쓰지 않는다.

**표제를 뺀 모든 면에 그림 또는 표를 둔다** — 산문 나열은 가독성이 떨어진다:

| | 내용 | 생성 방식 |
|---|---|---|
| 그림 1 | 층 분해 모식도 | matplotlib 합성 파형. **진폭은 파장이 짧을수록 작게** 줘야 합 패널에서 장파장 추세가 살아난다(등진폭이면 합이 Crustal 과 구분되지 않음) |
| 그림 2 | 층별 공간 파장 대역 | IGRF 차단차수 13 에서 λ=2πR/√(n(n+1)) 계산 |
| 그림 3 | 구축 절차 흐름도 | **pptx 네이티브 도형** — 발표자가 직접 편집 가능 |
| 그림 4 | 측점 분포 | `docs/data/lmm_model.json`(17점) + `data/korea_boundary.geojson` |
| 그림 5 | 산출 체계 | pptx 네이티브 도형 |
| 그림 6 | 요약 모식도 | matplotlib |

그림 1·6 은 모식도이므로 캡션에 실측이 아님을 명시한다.

| 이슈 | 대응 |
|---|---|
| **한글 세리프가 matplotlib 에 안 잡힘** | `NotoSerifKR-VF.ttf` 는 **가변 글꼴**이라 기본 스캔에서 누락되어 조용히 Batang 으로 대체된다(본문과 도판 글꼴 불일치). `font_manager.fontManager.addfont(경로)` 로 직접 등록할 것 |
| 한글 세리프 글리프 공백 | `≳`(U+2273) `⟶`(U+27F6) 등은 한글 세리프에 없다. 「3,000 km 이상」처럼 한글로 쓰거나 `≈` 수준의 상용 기호만 사용 |
| latin/ea 분리 지정 | Cambria 에 한글이 없으므로 run 마다 `<a:latin>` 과 `<a:ea>` 를 따로 물려야 한다 (`font_pair()`). `font.name` 만 쓰면 한글이 대체 글꼴로 떨어진다 |

이 문서는 **결과 보고가 아니라 절차(안)** 이므로 현 시점 LOO 등 적합 결과는 싣지 않는다.
`create_lmm_slides_mintlify.py` 는 Mintlify 디자인 시스템 판본으로, 참고용으로만 남겨 둔다.

자세한 내용은 아래 12절 참조.

---

## 3. 파일 경로 구조

```
/output/                        ← 중간 산출물 (git 제외, .gitignore)
  geomag_site_selection.html    ← 생성된 지도 HTML
  candidate_sites.csv
  data/
    candidates_p1/p2/p3.geojson
    gradient_low/mid_low/optimal/caution.geojson  ← 자기이상 등급별
    zone_*.geojson

/docs/                          ← GitHub Pages 배포 폴더 (git 커밋 대상)
  index.html                    ← build_docs.py가 output/HTML을 복사
  data/
    *.geojson                   ← build_docs.py가 단순화하여 복사
  output/
    YYYYMMDD_HHMMSS_*.docx      ← 방법론 문서
    *.csv, *.png 등 산출물
```

### 저장 경로 원칙
- 모든 산출물은 `docs/output/`에 저장 (git 커밋 대상)
- `docs/output/` 경로: `Path(__file__).parent / "docs" / "output"` (절대경로 하드코딩 금지)
- 로컬 백업: `C:/LG_gram_backup_users/LX/2026_geomag/docs/output/`
  - 두 경로가 동일한 경우(로컬 직접 실행 시) 복사 생략 (`resolve()` 비교)
- 루트 `/output/` 단독 저장 금지

### 파일명 규칙
- 날짜/시간 포함 파일: `YYYYMMDD_HHMMSS_설명.확장자`
  - 예: `20260514_153745_site_selection_methodology_geology.docx`

---

## 4. Git 워크플로우

### 커밋 대상
- `docs/` 폴더 전체 (HTML, GeoJSON, output 산출물, Word 문서)
- `output/`(루트)는 커밋하지 않는다 (`.gitignore` 처리됨)

### 표준 커밋 순서 (분석 결과 업데이트 시)
```powershell
python geomag_site_selection.py
python build_docs.py
git add docs/
git commit -m "분석 결과 업데이트"
git push origin HEAD:main
```

### Push 규칙
- 항상 `git push origin HEAD:main` 사용 (브랜치명 불일치 방지)
- non-fast-forward 오류 시: `git pull origin main --rebase` 후 재시도
- 워크트리가 뒤처진 경우도 동일하게 rebase

### .gitignore 핵심 규칙
- `/output/` — 루트 output만 제외 (`output/`로 쓰면 `docs/output/`도 차단되므로 주의)
- `docs/output/data/` — 대용량 원본 GeoJSON이 잘못 생성되는 폴더
- `data/*.json`, `data/*.csv`, `data/*.dat` — Overpass API 캐시, 대용량 원본
- `data/수치지질도_25만축척_전국/` — 대용량 shapefile
- `data/zone_cache/` — 제외구역 유니온 WKB 캐시 (생성된 바이너리, 커밋 불필요)

---

## 5. 입지 점수 산정 체계 (현재 구현 상태)

| # | 항목 | 배점 | 상태 | 산정 방식 |
|---|---|---|---|---|
| ① | 격자 데이터 희소성 | 25 | ✅ | KDTree K=5 최근접 이웃 평균 거리 정규화 |
| ② | 지형적 대표성 | 15 | ✅ | Open-Elevation API 경사도 (1 - slope/30°) × 15 |
| ③ | 전력/철도 이격도 | 15 | ✅ | log(dist) 함수 기반 정규화 |
| ④ | 인구 밀집 이격도 | 15 | ✅ | log(dist) 함수 기반 정규화 |
| ⑤ | 자기이상·지질경계 모델 기여도 | 10 | ✅ | KIGAM 반경 0.05° P90-P10 구간별 점수화 |
| ⑥ | 암상 적합성 | 5 | ✅ | 수치지질도(1:250,000) 자성암종·단층 이격거리 log 정규화 |
| ⑦ | 부지 지속성 | 10 | ❌ | 현장 확인 (미구현) |
| ⑧ | 관리 접근성 | 5 | ❌ | 현장 확인 (미구현) |

- **최대 가용 점수**: 85점 (①~⑥) → 100점 정규화
- **우선순위 등급**: P66 이상 → 1등급, P33~P66 → 2등급, P33 미만 → 3등급

### ⑤ 자기이상 점수 구간
| P90-P10 | 점수 |
|---|---|
| < 30 nT | 5점 |
| 30~150 nT | 8점 |
| 150~400 nT | 10점 (최고) |
| 400~800 nT | 7점 (현장 검증 필수) |
| > 800 nT | 조건부 제외 |
| 데이터 미취득 | 6점 |

---

## 6. 제외 구역 필터 (사전 제거)

| # | 조건 | 반경 |
|---|---|---|
| [1] | 고압철탑·송전탑 | 1.0 km |
| [2] | 철도 | 5.0 km |
| [3a] | 핵산도심·산업 | 500 m |
| [3b] | 주거·하락 | 300 m |
| [4] | 파이프라인 | 0.5 km |
| [5] | 풍신탑·기지국 | 0.5 km |
| [6] | 풍력발전기 | 0.5 km |
| [7] | 채석장·광산 | 1.0 km |
| [8] | 자기이상 P90-P10 >800 nT | 조건부 제외 |
| [지질] | 자성 암종 폴리곤 내부 | 직접 제외 |
| [지질] | 단층선 | 500 m |

---

## 7. 지도 레이어 구성

### 자기이상·지질경계 모델 기여도 레이어 (기본 비표시, 레이어 컨트롤에서 활성화)
| 레이어 | 색상 | 파일 |
|---|---|---|
| < 30 nT (복잡 작용) | 회색 #B0B0B0 | `docs/data/gradient_low.geojson` |
| 30~150 nT (지-중) | 하늘 #88CCEE | `docs/data/gradient_mid_low.geojson` |
| 150~400 nT (최적) ★ | 녹색 #2E8B57 | `docs/data/gradient_optimal.geojson` |
| 400~800 nT (검증 필요) | 황색 #DDAA00 | `docs/data/gradient_caution.geojson` |

### 팝업에 표시되는 점수 컬럼
`s1`(희소성) · `s2`(지형) · `s3`(전력철도) · `s4`(인구) · `s5`(모델기여도) · `s6`(암상)
- `vn()` 함수로 NaN → `-` 처리
- `s6`은 `rename_map`에 `"s6_암상": "s6"` 포함되어야 GeoJSON에 저장됨

---

## 8. 방법론 문서 (`create_methodology_doc.py`)

- 저장: `docs/output/YYYYMMDD_HHMMSS_site_selection_methodology_geology.docx`
- 날짜: `datetime.now().strftime("%Y년 %m월")` 자동 생성 (하드코딩 금지)
- 로컬 백업: `Path("C:/LG_gram_backup_users/LX/2026_geomag/docs/output")`
  - 동일 경로 복사 방지: `if docs_path.resolve() != local_path.resolve()`
- 문서 내용이 바뀌면 재실행 후 `git add docs/output/` → 커밋

---

## 9. Python 환경

- 인터프리터: `C:\Users\YOONS\anaconda3\python.exe`
- 실행 예시: `& "C:\Users\YOONS\anaconda3\python.exe" geomag_site_selection.py`
- 주요 패키지: geopandas, shapely, folium, numpy, pandas, requests, scipy, python-docx
- 좌표계: WGS84 (EPSG:4326) 입출력, UTM Zone 52N (EPSG:32652) 공간 분석

---

## 10. 성능 최적화 — 제외구역 캐시

`build_exclusion_zones()` 내 `unary_union`은 주거 1.17M · 수계 1.44M 피처로 약 9분 소요.
두 번째 실행부터 `data/zone_cache/` WKB 캐시를 사용해 수 초로 단축.

### 캐시 작동 방식
- `_load_zone_cache(name, source_paths, buffer_key)`: WKB + meta JSON 확인 → 소스 파일 mtime 비교 → 유효하면 geometry 반환
- `_save_zone_cache(name, geom, source_paths, buffer_key)`: WKB + meta JSON 저장
- `build_exclusion_zones()` 내 `_cached()` 클로저가 자동으로 캐시 시도 → miss 시 계산 → 저장

### 캐시 무효화 조건
- 소스 JSON 파일 수정 시간이 캐시보다 최신인 경우
- `buffer_key` (버퍼 반경 또는 임계값 문자열) 변경 시
- `data/zone_cache/*.wkb` 수동 삭제 시 (재계산 강제)

### 캐시 경로
`data/zone_cache/` — `.gitignore`에 포함, 커밋하지 않음

---

## 11. 자주 발생하는 문제 및 해결

| 문제 | 원인 | 해결 |
|---|---|---|
| `git push` non-fast-forward | 브랜치 불일치 | `git pull origin main --rebase` 후 재push |
| `docs/output` ignored 경고 | `.gitignore`의 `output/` 패턴이 하위 경로도 차단 | `/output/`로 루트 한정 |
| `create_methodology_doc.py` PermissionError | 동일 경로 자기복사 | `resolve()` 비교 후 동일하면 복사 생략 |
| push 타임아웃 (HTTP 408) | 대용량 파일 포함 | `docs/output/data/` 경로 확인 및 제거 |
| 팝업에 암상 `-` 표시 | `rename_map`에 `s6_암상` 누락 | `"s6_암상": "s6"` 추가 확인 |
| gradient 레이어 컨트롤 미표시 | `anomaly_tiers_available=False` | KIGAM 데이터 존재 확인 후 재실행 |

---

## 12. LMM (한반도 지역 자기장 모델) 계산기

오석훈 교수 설명자료(`lmm_korea_explainer_kr.pdf`)의 4-층 결합 구조를 구현.

```
B_LMM(r,t) = B_IGRF + B_Regional + B_Crustal + B_External
```

### 층별 자료원 및 보유 현황

| Layer | 자료원 | 파일 | 보유 |
|---|---|---|---|
| ① Core | IGRF-14 (13차) | `ppigrf` 패키지의 `IGRF14.shc` | ✅ |
| ② Regional | 지표 절대측정 D·I·F | 성과표(22~25) + 2019 야장 | ✅ **17측점** |
| ③ Crustal | KIGAM 자력이상도 (+ **벡터 복원**) | `data/mag_1982-2018_1.5min_ed.dat` | ✅ 2.8 km 격자 · D·I 에도 기여 |
| ④ External | 관측소 **4소** (청양 CYG + KASA 제주·강릉·이천) | `data/cyg/` · `data/kasa/` | ✅ **편각에 적용**(성과 정리 단계) |

`INCLUDE_2019 = True` (lmm_build.py) 로 2019 야장 검증 통과분을 포함한다.

⚠️ **2026-08-19 확정 — External 은 «봉쇄»가 아니라 «효과가 안 보이는» 상태다.**

표현을 바로잡는다. 자료는 **전부 있다** — 관측시각(212세션) · CYG 2019~2025 ·
KASA 3소 · Kp. 보정량도 실제로 산출했다(196세션, F 58 nT · D 7.2′ RMS).
문제는 그 보정량이 **잔차와 상관이 거의 없다**는 것이다.

| 성분 | 보정량 RMS | IGRF 잔차 RMS | 보정량 vs 잔차 상관 | 외부장이 설명하는 분산 |
|---|---|---|---|---|
| F | 58 nT | 190 nT | **r = +0.02** | **9%** |
| D | 7.2′ | 33′ | **r = +0.15** | **5%** |

즉 **완벽히 보정해도 F 분산의 9%, D 의 5%밖에 못 걷어낸다.** 나머지 91~95% 는
다른 원인(측점 계통오차 · 지각장 해상도)이고, 상관이 0 에 가까운 값을 빼면
잡음만 더해진다 — LOO 가 악화되는 이유가 이것이다.

→ **External 은 KPI 달성의 열쇠가 아니다.** 우선순위를 이렇게 다시 잡는다:
① 편각 35′ 계통오차(성과표 감사) ② 지각장 해상도 ③ External(NOC 공간투영).
NOC 는 관측소 4곳으로 **구현 가능하지만**, 성공해도 위 몫 이상은 못 얻는다.

야장 212세션으로 2019~2025 전 구간 시각을 확보하고(`lmm_fieldbook`), 세션별 외부장
편차를 산출해(`lmm_external` → `docs/data/external_corrections.csv`, 196세션) 성과값에
걸어 **end-to-end 재적합**했다(`compare_external_modes.py`, degree 1 · 생산 설정):

| 모드 | 표본 | LOO D | LOO I | LOO F | 판정 |
|---|---|---|---|---|---|
| **none (채택)** | 17 | **0.7691°** | **0.1787°** | **60.26 nT** | 기준선 |
| drop_storm (Kp>2 성과 배제) | 14 | 0.7430° | 0.2846° | 63.28 nT | 표본 14로 감소 · I 크게 악화 |
| subtract (전 세션 평균 차감) | 17 | 0.8481° | 0.1787° | 68.99 nT | **악화** |
| subtract_quiet (정온만) | 17 | 0.9008° | 0.1787° | 74.85 nT | **더 악화** |

**차감할수록 나빠진다.** |dF| 평균 38 nT 를 빼는데 LOO F 가 +8.7 nT 늘어난다 —
보정량이 실제 오차와 상관이 없어 독립 잡음처럼 더해진다는 뜻이다. 2019 표본에서
관찰됐던 방향(LOO D 0.751→0.858)이 **7년치 표본에서 재현**됐다.

원인은 자료 부족이 아니라 **근사의 실패**다. CYG 한 곳의 편차를 전국에 동일 적용하는
균일 V 근사는 76~229 km 떨어진 측점에서 성립하지 않는다. 설명자료가 NOC **공간투영**을
요구하는 이유가 두 번째로 실증됐다.

⚠ `drop_storm` 의 D 개선(−0.026°)을 근거로 삼지 말 것 — 표본이 17→14 로 줄어 비교가
성립하지 않고, 같은 조작이 I 를 0.179→0.285° 로 악화시킨다.

→ **다음 요건은 자료가 아니라 NOC 공간투영 구현이다.** 그 전까지 `EXTERNAL_MODE="none"`.

---

**④ (종전 기록) 2019 구간 적용 방침** — 관측시각이 있는 2019 구간에만, **Kp>2 세션 배제**로 적용
(`FB2019_MAX_KP = 2.0`). **CYG 값 직접 차감은 하지 않는다** (`FB2019_APPLY_EXTERNAL = False`):

| 방식 | LOO D | LOO I | LOO F |
|---|---|---|---|
| ① 보정 없음 | 0.751° | 0.188° | 64.0 nT |
| **② Kp>2 배제 (채택)** | 0.766° | **0.173°** | **58.6 nT** |
| ③ CYG 직접 차감 | **0.858°** ❌ | 0.188° | 58.0 nT |
| ④ ②+③ | 0.858° ❌ | 0.173° | 65.8 nT |

③을 쓰지 않는 이유:
1. **편각 악화** — 「V 전역 균일」 근사는 편각에서 특히 취약(국소 H 방향 의존)
2. **F 기록 단위 불일치** — 야장 F 는 **일자별 단일값**(거제·장흥은 측점당 1개).
   세션 시각 보정량을 일자 대표값에서 빼는 것은 원리적으로 성립하지 않음.
   §19①6 은 세션마다 F 측정을 요구하나 야장 양식이 이를 담지 않는다.

②는 값을 조작하지 않고 오염 표본만 제외하므로 위 문제가 없고, 설명자료의
Kp≤2 조건과도 일치한다. 미원 2019-05-14 2세션(Kp 5.7~6.3)이 배제된다.

### 현재 성능 (Leave-One-Out 교차검증, 16측점 · 2026-08-19 최신)

⚠️ 아래 「감사 전/후」 열은 **순환 적합 기준**의 옛 기록이다. 정본은 위
「순환 적합 발견과 재판정」 절의 표다.

| 성분 | 감사 전 17점(순환) | 감사 후 16점(순환) | **재판정 정본** | KPI | 판정 |
|---|---|---|---|---|---|
| D | 0.7691° | 0.5403° | **0.5016°** | < 0.1° | ❌ |
| I | 0.1787° | 0.2600° | **0.0876°** | — | — |
| F | 60.26 nT | 58.41 nT | **61.4 nT** | < 50 nT | ❌ |
| 편각 잔차 RMS | 35.0′ | 20.73′ | **26.4′** | 6′(KPI) | ❌ |
| Crustal r | +0.513 | +0.508 | **+0.501** | — | n=13, 불안정 |

⚠️ **LOO I 0.0429° 를 성과로 읽지 말 것.** 지각 벡터를 빼자 I 잔차가 줄면서
robust 적합의 inlier 가 15→8 로 좁아졌고, LOO 는 그 8점 위에서만 계산된다.
**inlier 집합을 고정한 공정 비교값은 0.2600 → 0.2408°(−7%)** 이다. 편각(D)은
같은 공정 비교에서 0.5403 → 0.3396° 로 줄어 선택효과가 아니다.

**여전히 KPI 미달 — 정식 편각 산출에 사용 금지.** 남은 원인:
1. Crustal 격자 해상도 부족 (2.8 km). 설명자료 사례연구는 측선간격 250 m 원자료 사용.
   복원 벡터의 편각 예측이 잔차의 61%(8.38′/13.75′)를 차지하나 최적배율이 1.6 이라
   **격자 평활로 진폭이 과소평가**되고 있다. ⚠️ **원측선 자료는 존재하지 않으므로
   (2026-08-19 확인) 이 몫은 현 자료로 채울 수 없다** — 현 격자가 해상도 상한이다.
2. 지표 측점 16개 (권고 30점 미달) + 방문마다 달라지는 방위 기준
3. External 은 관측소 4소로 근사일 뿐 NOC 모드 분해가 아니다

⚠️ Crustal 상관 r 은 표본이 11개뿐이라 측점 하나만 바뀌어도 0.16~0.51 로 흔들린다.
개선을 단정하지 말 것.

### 자료 처리 시 주의사항 (실제 발생 이슈)

| 이슈 | 내용 |
|---|---|
| **KIGAM 위도축 비균일** | 제주해협(33.55°~34.1°)에 0.55° 공백 존재. `np.unique()` 결과를 그대로 축으로 쓰면 `(값-원점)/간격` 색인이 공백 위쪽 **전 구간에서 21행 어긋난다**. `load_kigam_grid()` 는 결측행을 NaN 으로 채운 균일축을 생성 |
| **양산/언양 중복** | 동일 지점이 두 이름으로 기재됨. 좌표 4자리 반올림 후 중복 제거 |
| **편각 블런더** | 삼척·가야 등은 재방문 편각 산포가 35~67 arcmin 으로 영년변화(최대 15')로 설명 불가 → `MAX_D_SPREAD_ARCMIN` 로 배제 |
| **inlier 선택편향** | Crustal 보정 후 잔차로 inlier 를 고르면 Crustal 기여도가 부풀려진다. 반드시 **IGRF 잔차만으로** inlier 판정 |
| **높이 기준 불일치** | `ppigrf.igrf(h=)` 는 **타원체고**를 요구하나 성과표에는 **표고**만 있다. `H = h − N`, 한반도 N ≈ 20~30 m (KNGeoid). 다만 지오이드는 완만해 Regional 상수항에 흡수된다 — N 을 20~30 m 부여해도 **LOO 는 58.64 nT 로 불변**, Regional F 상수항만 약 0.7 nT 이동. 높이 감도 **F −0.026 nT/m**, D +13.9″/km. 계산기 입력은 **표고로 통일**할 것 |

### 웹 계산기 분포도 (`docs/lmm.html`)

지도는 외부 타일 없이 `data/korea_boundary.geojson` 해안선을 인라인해 캔버스로 직접 그린다
(오프라인 단일 파일 원칙 유지). 표시 항목: 편각 D · 복각 I · 총자력 F · 지각 자기이상 · LMM−IGRF.

| 구현 요점 | 내용 |
|---|---|
| **IGRF 사전격자** | 0.25° 격자에서 구면조화 합성 1회 → 화소별 쌍선형 보간. Crustal 층만 원해상도 조회 |
| **육지 마스크 필수** | `putImageData` 는 **clip 경로를 무시**한다(캔버스 사양). 별도 마스크 캔버스를 만들어 해상 화소 알파를 0으로 처리하고, 색범위도 육지 화소에서만 산정 |
| **분위수 색범위** | 2~98 백분위로 잘라 극단 이상값이 색범위를 독점하지 않게 함 |
| **자료 공백 표시** | 항공자력 미측선 구역은 45° 빗금 — 0 이 적용된 구간임을 시각적으로 명시 |
| **NaN 은 두 종류** | `MAP.vals` 의 NaN 은 ① 해상(육지 마스크 밖) ② 육지이나 항공자력 공백 두 원인이 있다. 렌더링은 구분하지만(투명 vs 빗금) **툴팁이 둘 다 "자료 공백"으로 표시하던 버그**가 있었다. `MAP.mask` 로 판별할 것 |
| **툴팁 리사이즈** | 툴팁이 뜬 상태로 리사이즈·화면회전하면 이전 폭 기준 `left` 가 남아 좁은 화면에서 문서 가로스크롤을 만든다. resize 핸들러에서 즉시 감출 것 |
| **색상** | 순차형(D·I·F)은 단일 색조 blue 램프, 발산형(자기이상·차이)은 blue↔red + 회색 중간 |

### 관측소 영년변화 뷰어 (`docs/lmm.html` 내장, `build_sv_viewer.py` + `sv_server.py`)

CYG·강릉·제주·이천 4관측소의 X·Y·Z·F 시계열 대화형 뷰어. **두 모드**:
- **일 중앙값·장기** — lmm.html 에 **내장**(오프라인). 일 중앙값 ~2,557일 × 16계열(JSON ~230 KB).
  성분 탭·연도 버튼·기간 좁히기·관측소 토글.
- **분 단위·월별** — `sv_server.py` 의 `/api/minute?start=YYYY-MM&end=YYYY-MM` 에서 **월 범위**
  (최대 12개월, `MAX_MONTHS`) 분자료를 on-demand **fetch**. 시작/종료 월 입력 + 로드 후 시작/종료일로
  구간 좁히기. 분 단위(3.6M 행)는 파일에 못 넣으므로 서버로 조회한다. 서버 없이(순수 오프라인)에는
  안내 메시지로 폴백하고 일 모드만 동작. (`ym=` 단일 월도 하위호환)

```
python kasa_geomag.py ...        # 관측소 자료 갱신
python lmm_cyg.py --start 2019-01-01 --end 2025-12-31   # CYG 갱신
python build_sv_viewer.py        # 일 중앙값 재집계 → lmm.html 재삽입(멱등, 마커 SV_CARD/SV_JS)
python sv_server.py              # http://127.0.0.1:8899/lmm.html (분 단위 모드 활성화)
```

`sv_server.py`: `docs/` 정적 서빙 + `/api/minute` (KASA 연도파일·CYG 일자파일을 월 단위로 읽어
1분 격자 정렬 JSON 반환, `lru_cache` 로 로드 캐시). 분 모드는 반드시 이 서버 주소로 lmm.html 을
열어야 동작한다.

| 유의점 | 내용 |
|---|---|
| **멱등 삽입** | `<!-- SV_CARD -->`·`<!-- SV_JS -->` 마커로 카드·스크립트를 교체. CYG 수집이 갱신되면 `build_sv_viewer.py` 만 재실행 |
| **JS 문자열의 `\n` 금지** | Python 소스에 JS 문자열 `"\n"` 을 넣으면 파일에 **실제 개행**으로 들어가 문자열이 끊겨 "Invalid or unexpected token" 이 난다. 캔버스 라벨은 개행 없이 조립할 것(월/연 라벨 분기) |
| **캔버스 폭 초기화** | `<canvas>` 는 CSS `width:100%` + 최초 `requestAnimationFrame(draw)` 로 레이아웃 확정 후 재렌더해야 기본 300px 로 굳지 않는다 |
| **성분 규약** | 강릉·제주 X·Y 는 계기(변화계) 좌표(Y≈0) — 절대 편각 아님. 툴팁·라벨에 주의문구 포함 |
| **디버그** | Blob 스크립트 + `window.onerror` 로 삽입 스크립트의 컴파일 오류 줄·열을 잡을 수 있다(브라우저 팬 미표시 시 스크린샷 불가하므로 픽셀·상태를 JS 로 검증) |

### 관련 법령 (2026-07 조사)

⚠️ **편각 실체 규정은 지도 계열이 아니라 측량 계열에 있다.**
「지구물리측량 작업규정」(국토지리정보원고시 제2021-2985호) 제3장이 핵심.
지도 계열(지도도식규칙·도식적용규정·수치지형도 규정)에는 편각 정확도 조항이 없다.

**「지구물리측량 작업규정」제3장 지자기측량 (제12~21조)**

| 조문 | 내용 |
|---|---|
| §12① | **1등 지자기점 = 「국가기본도의 자편각 표기」 목적**으로 설치 (법정 목적) |
| §13② | 이격거리 — 직류철도 5.0 / 교류·일반철도 2.0 / 고압철탑 1.0 / 송전탑 0.5 km |
| §17 | 총자기장 측정기 **0.1 nT** 단위 판독 |
| §19① | 1등 **1일 6회↑** / 2등 4회↑, 편각은 지구물리학적 진북 기준, **편각·복각 측정 시 총자기장과 시간 동시 측정** |
| **§20** | **측정오차 한계 — 편각·복각 정수차 30′(=0.5°), 관측시간 20분 이내** |
| **§21** | **상시 기준점과 비교해 일변화·영년변화 보정 후 기준년 환산** |

⚠️ §13·§20 의 표는 **PDF 안에 이미지로 들어있어 텍스트 추출이 안 된다.**
`fitz`(pymupdf)로 렌더링해서 읽어야 한다.

**KPI 와 법정 기준의 관계** — `D<0.1°`(=6′)는 설명자료의 **공학적 목표치**이고,
법정 §20 의 30′는 **측정 품질관리 기준**이라 성격이 다르다. KPI 가 5배 엄격하므로
**KPI 미달은 법령 위반이 아니다.** 역으로 법정 30′ 충족이 모델 투입 적합을 뜻하지도
않는다 — 포천(22.3′)은 법정 이내지만 포함 시 LOO D 가 0.751°→0.807° 로 악화된다.
그래서 `MAX_D_SPREAD_ARCMIN = 20` 을 쓴다.

**자침편차 표기** — §12① 로 **법정 목적**은 성립하나, 도식 규정상 도표 자체는
난외사항 필수목록에 없어 **표기는 임의사항**이고 적용 축척도 특정되지 않는다
(부령 §8 열거 제외, 고시 §205 목록 제외).

**도폭 규격은 법정 사항**: 도식적용규정 §4·§211 — 1:25,000 = 경위도 **7′30″(0.125°)**,
1:50,000 = 15′(0.25°).

**1:25,000 을 기준으로 삼는 근거는 공학적 계산이다.** 도폭당 자침편차를 단일 값으로
표기하려면 도폭 내 편각 변화가 표기 정밀도보다 작아야 하는데, IGRF-14 로 계산하면:

| 축척 | 도폭 내 D 변화 | 0.1° 예산 대비 |
|---|---|---|
| 1:50,000 | 0.0905° | 91% (여유 없음) |
| **1:25,000** | **0.0453°** | **45%** |
| 1:5,000 | 0.0091° | 9% |

→ 모델 오차분을 남길 수 있는 가장 성긴 축척이 1:25,000.
계산은 `create_lmm_report.py: sheet_scale_table()` 에서 재현된다(하드코딩 아님).

⚠️ 산출물에 "1:25,000 도폭 편각 정식 산출 금지" 처럼 **법정 요건으로 읽히는 표현을
쓰지 말 것**. "지형도 자침편차 등 정식 편각 산출" 로 표기한다.

✅ **확정 — 성과표는 야장 세션평균 그대로인 원시값** (`ASSUME_NO_DIURNAL_CORRECTION = True`,
2026-08-19 실증 확인. `audit_survey_table.py`)

종전에는 「일변화 보정 미적용」을 정황 근거로 잠정 처리했으나, 야장을 확보해 **직접 대조로
확정**했다. 성과표와 야장 세션평균이 매칭 18행 전부에서 표기 자릿수 이내로 일치한다:

| 성분 | 중앙 | RMS | 최대 |
|---|---|---|---|
| ΔD | −0.00′ | **0.00′** | 0.00′ |
| ΔI | +0.00′ | **0.03′** | 0.14′ |
| ΔF | +0.00 nT | **0.02 nT** | 0.05 nT |

→ **기준시점 환산도, 일변화 보정도 적용되지 않았다.** 성과표는 야장의 「전자력(평균)」과
세션 편각·복각 평균을 그대로 옮긴 값이다(강화 2024 F = 51531.185 가 야장 평균과 동일).

이로써 두 가지가 풀린다:
1. **실제 관측일시로 영년변화를 거는 것이 옳다** — 이중보정이 아니다. `lmm_build` 의
   야장 일시 반영 후 LOO 미세 악화(F 58.6→60.3 nT)는 **잡음으로 확정**.
2. **External 보정을 적용할 근거가 섰다** — 보정되지 않은 원시값이므로 뺄 것이 남아 있다.

⚠ **검정 ②(반복측정 영년변화 대조)는 판별력이 없다** — 성과 ΔD 가 −43′~+60′ 로 흩어지는데
IGRF 영년변화는 −7′ 라, 잔차 35′ 가 신호를 덮는다. 이 표의 「원시값 6 · 환산값 4」를
근거로 읽지 말 것. 판정은 검정 ① 단독으로 성립한다.

⚠ **야장 셀 서식 오류 1건** — 미원 2024 5회차 복각이 `1900-01-04 00:00:00`(날짜)로 저장돼
있다. `parse_results()` 가 한반도 범위(D ±20° · I 30~70° · F 30~60k nT)로 걸러낸다.

또한 §19①6 이 시각 동시 측정을 의무화하므로 **2022~2025 야장에는 시각이 있어야 한다**
(성과표에만 누락) — 확보하여 확인 완료.

⚠ **야장 시각은 KST**. CYG(UTC)·Kp(UT)와 결합할 때 반드시 −9h 변환할 것. KASA 캐시는
`time_kst` 라 KST 그대로다 — 세 자료의 시간대가 서로 다르니 결합 지점마다 확인한다.

미확인: 국토지리정보원 기관표준 「기본공간정보 데이터 품질」(축척별 RMSE 허용치),
실제 1:25,000 도엽의 자침편차 인쇄 여부·표기 자릿수.

### 검증 체계

세 구현(Python·JavaScript·Excel)이 동일 좌표에서 일치함을 확인:
- JS ↔ Python: D 0.22", F 0.27 nT 이내
- Excel ↔ Python: D 0.33", F 0.12 nT 이내 (사전격자 보간오차)
- JS IGRF 엔진 ↔ ppigrf: D 0.17", F 0.03 nT 이내

`formulas` 패키지로 xlsx 수식을 Python 에서 평가해 회귀검증 가능.

### ④ External 층 — 대체 상시관측소 (KASA, `kasa_geomag.py`)

자문회의 권고(청양 단독 지양, 이천·소백산·제주 등 대체관측소 활용)에 따라 우주환경
빅데이터 플랫폼(spaceweather.kasa.go.kr)에서 **제주·강릉·이천·보현산** 지자기 분자료를
받아 `data/kasa/` 에 캐시한다. **인증키·로그인 불필요.**

```
python kasa_geomag.py --probe                                  # 연도별 커버리지 조사
python kasa_geomag.py --station three --start 2019-05-04 --end 2019-05-31
python kasa_geomag.py --station boh   --start 2020-07-01 --end 2020-07-31
```

| 이슈 | 내용 |
|---|---|
| **두 경로** | Open API(`/api/magnetism/*`)는 **최근 ~1일 롤링**·날짜 파라미터 없음(실시간용). 과거자료는 데이터셋 다운로드(`dataSetSampleListExcelDownAjax.do` → `fileDownload.do`) |
| **XLSX 위장** | 파일명은 `.csv` 지만 실제는 **XLSX(ZIP)**. inlineStr + `dimension` 태그 깨짐 → openpyxl read_only 실패. `_parse_row()` 가 **셀 r= 열문자 기준**으로 파싱(빈 셀 열밀림 방지) |
| **요청 상한** | 3관측소(제주·강릉·이천)=5일, 보현산=1개월. 스크립트가 자동 분할 |
| **실제 커버리지 ≠ 포털 표기** | 포털 "제공기간 2022-06-14"는 등록일. **3관측소 실데이터는 2018년부터** — **2019 야장 보정 가능**. `data/kasa/README_coverage.md` 참조 |
| **성분 규약 상이** | 이천 Y는 지리 정북 기준(D≈−8°), **강릉·제주 Y는 센서/변화계 기준(Y≈0)** — 일변화 편차 보정엔 무방하나 **절대 편각 환산 금지**. F는 세 곳 모두 벡터크기 정합 |
| **보현산 무효 구간** | 2019 전 구간 H=D=Z=11647.42 **상수 placeholder(무효)**. 실측은 2020 일부만. 2021~2025 사실상 없음 → 단독 보정 부적합, 참고용 |
| **결측/시각** | 결측 `-99999`. 시각 **KST**(CYG·Kp는 UT → 결합 시 −9h). 캐시 `data/kasa/*.csv` 는 `.gitignore`, README 는 커밋 |

IGRF epoch 분리: 2019 야장→2015–2020(IGRF-13), 2020+ 성과→2020–2025(IGRF-14). 각 epoch에
대응하는 관측소 기준자료를 위 스크립트로 창별 수집한다.

### ④ External 층 — CYG 자료 수집 (`lmm_cyg.py`)

INTERMAGNET HAPI 서버에서 청양(CYG) 1분 자료를 받아 `data/cyg/` 에 캐시한다.

```
python lmm_cyg.py --start 2024-06-01 --end 2024-06-30 --report
```

**등급별 CYG 보유 기간 (2026-07 확인)**

| 등급 | 기간 | 측정기간(2022~2025) 포함 |
|---|---|---|
| definitive | 2014-01-01 ~ 2017-12-31 | ❌ |
| quasi-def | 2015-04-25 ~ 2020-12-31 | ❌ |
| adjusted / best-avail | 2013-10-30 ~ 현재 | ✅ |

→ **정의치(definitive)는 우리 측정기간을 못 덮는다.** `best-avail`(일자별 최상위 등급
자동 선택)을 쓰되, 보고서에 사용 등급을 반드시 명시할 것.

**주의사항**

| 이슈 | 내용 |
|---|---|
| **결측 표식** | 99999.0 이 결측값. 실수로 그대로 쓰면 치명적 → `fetch_day()` 에서 NaN 변환 |
| **레코드 수 ≠ 유효** | 결측일도 1440행이 반환된다. 반드시 유효 카운트로 판정 |
| **관측소 장기 결측** | 2025-09-08~22 이상 전량 결측 확인. 측정일이 이 구간이면 보정 불가 |
| **캐시 제외** | `data/cyg/` 는 `.gitignore` 처리. gitignore 는 **행말 주석을 지원하지 않으므로** 주석은 별도 행에 |

**측정된 외부장 일변동 (2022~2025 표본)**

| 성분 | 중앙값 | 최대 | KPI |
|---|---|---|---|
| F | 37.5 nT | 68.4 nT | < 50 nT |
| D | 0.112° | 0.220° | < 0.1° |

→ **D 는 전형적인 하루 변동만으로도 KPI 를 초과한다.** 측정시각 보정 없이는
편각 KPI 달성이 원리적으로 불가능함을 실측으로 확인.

### 야장 파싱 및 외부장 보정 (`lmm_fieldbook.py`, `lmm_external.py`)

```
python lmm_fieldbook.py   # 야장 -> 관측 일시 복원
python lmm_external.py    # 야장 일시 x CYG x Kp -> 외부장 보정량
```

성과표에는 연도만 있으므로 **야장(MAG-01H 양식)에서 분 단위 관측시각을 복원**한다.
`docs/data/*야장*.xlsx` 와 **지리원 야장 원본 트리**(`NGII_FIELDBOOK_DIR`)를 함께 수집한다.

### 지리원 야장 원본 확보 (2026-08) — External 봉쇄 해제

```
D:\LX_yoons\2026_research\2026_지자기 연구\20260811_지리원 야장 자료\지구물리측량\지자기측량
  ├ 2020지자기 / 2021지자기 / … / 2026 지자기      (연도 폴더)
  └ 각 연도 아래 「1. 성산」「2. 순천」… 측점 폴더
```

**2022~2025 야장에 분 단위 관측시각이 그대로 있다.** 「2022~25는 연도만 있어 External
적용 불가」라던 봉쇄가 풀린다(13절 작업순서의 0단계). 기존 `parse_sheet()` 가 **무수정으로
읽힌다** — 2019 야장과 같은 양식이다.

| 항목 | 내용 |
|---|---|
| 수집 결과 | **212 고유 세션 · 18측점 · 2019~2025**, 시각 미기입은 성산 2019 **1건뿐** |
| 연도별 | 2019: 6측점 23세션 · 2020: 6/39 · 2022: 2/13 · 2023: 7/50 · 2024: 7/43 · 2025: 7/44 |
| 없는 것 | **2021 전체**(사진·`지자기측량 성과.xlsx`만) · 2023 경주(측정 못함) · 2024 삼척(zip 사진만) |
| 산출 | `docs/data/fieldbook_sessions.csv` (일시·측점만 담은 파생자료, 원자료 아님) |

⚠ **파일명이 「야장」을 안 담은 것도 있다**(2024 거제 `배포.지자기(거제)점….xlsx`).
확장자로 훑고 내용으로 판별할 것. 점조서(`★.계속~지자기 점조서…`)는 20~28 MB 라
`NGII_SKIP_WORDS`·`NGII_MAX_BYTES` 로 제외한다.

**성과표 대조** — 성과표 25행 중 **18행에 실제 관측일시를 붙일 수 있다**(`_fb_match.py`).
미매칭 7행은 2022 성과 6건(강화·거제·미원·삼척·양산·이원)과 2024 삼척인데, 2022 성과의
실제 관측은 2021년 캠페인(안동·진도·거창·강화 차수)으로 보이고 그 해 야장이 없다.
역으로 **야장에만 있고 성과표에 없는 (측점,연도) 17건**(2020 6측점·임계2022·양산2024·
부안/상주/함양 2025 등)은 Regional 입력 확대 후보다 — 야장 자체에 D·I·F 가 있으므로
`lmm_verify2019` 방식으로 검증하면 투입할 수 있다.

**`lmm_build.attach_fieldbook_datetime()`** 이 (측점,연도) 로 맞춰 7/1 대체일을 실제
일시로 바꾼다(세션 시작시각 **중앙값**을 대표로 — 성과가 세션 평균이라 특정 세션을 고를
근거가 없다). 세션표가 없으면 종전 7/1 대체로 폴백한다.

⚠️ **실일시 반영 후 LOO 가 미세하게 나빠졌다** — D 0.7661→0.7691° · I 0.1729→0.1787° ·
F 58.64→60.26 nT (Crustal 상관은 0.508→0.513 로 소폭 개선). **이것을 개선/악화로 읽지
말 것**: 변화량이 잔차 수준(D 35′·F 60 nT)의 수십분의 일이라 잡음 안이다. 다만 성과표가
이미 **기준시점 환산값**이라면 실일시로 영년변화를 다시 거는 것이 이중보정이 되므로,
이 부호는 **「성과표 원본 감사」(13절 1단계)에서 확인할 항목**으로 남긴다. 실일시를
유지하는 이유는 정확도 개선이 아니라 **External 층 적용의 전제**이기 때문이다.

| 야장 양식 특징 | 대응 |
|---|---|
| 세로 블록 반복 + 가로 3열 간격 회차 | 라벨 탐색(고정 좌표 금지). 행 위치가 파일마다 1~2행 밀림 |
| `일자` 표 뒤에 `측정일자` 표가 이어짐 | **둘 다 블록 경계로 처리**. 놓치면 뒷 표 시각이 앞 세션에 섞임 |
| 시각 미기입은 `00:00:00` | 결측으로 판정 (성산 2019-09-18 이 해당) |
| 편각/복각 시각을 분리 수집 | 장흥 2019-10-22 6회차는 복각이 편각보다 57분 빠름 — 야장 기입 오류 |
| 야장 F 는 **일자별 단일값** | 세션별 F 가 아니므로 "보정 전후 산포" 비교는 성립하지 않음 |

**외부장 보정 방식** — 정온야간 기준선(Kp≤2, 지방시 23~03시, 관측일 ±10일) 대비
세션 구간 평균 편차. 1차 근사로 V 가 전역 동일하다고 본다(NOC 공간투영 미구현).

**시각 처리** — 야장 시각은 KST. CYG·Kp 는 UT 이므로 반드시 변환할 것
(검증: 13:24 KST → 04:24 UTC → Kp 5.667).

### 2019 야장 6건 적용 결과 (방법론 검증)

| 항목 | 결과 |
|---|---|
| 복원 세션 | 22/23건 (성산만 시각 미기입) |
| 사용 등급 | **quasi-def** (2019 는 정의치 범위 밖, quasi-def 범위 안) |
| 보정량 F | 평균 14.9 nT, 최대 58.5 nT |
| 보정량 D | 평균 2.15′, 최대 5.22′ (0.087°) |
| 시각 미상시 잔존오차 | 동일 측점·동일일 세션 간 보정량 차 최대 **45.9 nT** (미원 5/14) |
| 자기폭풍 관측 | **미원 2019-05-14 2세션이 Kp 5.7~6.3 중 관측** — 성과 신뢰도 재검토 필요 |

폭풍 판정은 CYG 로 독립 검증됨: 5/14 X변동 118 nT vs 인근 정온일 27~37 nT.

⚠️ **2019 야장은 성과표(2022~2025)와 시기가 다르다.** 장흥·남지는 현재 15측점에
없고, 겹치는 거제·성산·이원·미원도 2019 관측은 성과표에 미포함.
즉 위 결과는 **파이프라인 검증**이며, 실제 모델 개선에는 **2022~2025 야장이 필요**하다.

### 2019 야장 신뢰도 검증 (`lmm_verify2019.py`)

Regional 층 투입 전 검증. **각도 단위가 곤(gon, 1회전 400)** 임에 주의 —
대척은 180°가 아니라 200 gon 이고, 야장 수식의 180/360 은 실제 200/400 이다.
도 환산 ×0.9.

```
편각 D[gon] = 참방위각[gon] - 마크평균 + (DEC1 - 200) - 100
복각 I[gon] = [(200-SU) + (400-ND) + (SD-200) + NU] / 4
```

편각은 마크 시준 방향에 따라 180° 분기가 생기므로 **|D| < 90° 분기를 택한다**.

**[내부검증] 22/22 세션 전부 통과**

| 항목 | 최대 편차 | 판정 |
|---|---|---|
| 마크 폐합 (200 gon) | 0.0116 gon (≈37″) | 통과 |
| 편각 재계산 vs 야장 | 0.0001° | 통과 |
| 복각 재계산 vs 야장 | 0.0000° | 통과 |
| 분력 정합 F²=H²+Z² | 0.0059 nT | 통과 |

**[외부검증]**

| 검증 | 결과 |
|---|---|
| 세션 반복성(원자료) | D 산포 최대 2.00′ = 0.033° — **KPI 0.1° 대비 양호** |
| CYG 보정 효과 | 정온시 D 악화 0.15′/I 개선 0.13′ (실익 없음), **교란시 I 0.01′→4.39′ 악화** |
| 2022~25 대조(IGRF 잔차) | 미원 **0.003°**, 이원 0.151°, 거제 0.225° |

⚠️ **naive 외부장 보정의 한계 확인** — CYG 로부터 76~229 km 떨어진 측점에
"V 가 전역 동일"이라는 1차 근사를 적용하면, 이미 산포가 2′ 수준인 자료에서는
개선이 없고 자기폭풍 시에는 오히려 악화된다. 설명자료가 NOC **공간투영**을
요구하는 이유가 실측으로 확인됨.

**좌표 대조** — 거제 9 m, 성산 4 m, 이원 5 m 로 동일 표석.
단 **미원은 353 m 불일치**(`'10~'19` 36°43′19″ vs 성과표 36°43′30″) — 현장 확인 필요.
겹치는 측점은 성과표 좌표를 기준으로 삼는다.

### 다음 단계 (KPI 달성 조건) — 2026-08-19 갱신

1. ~~CYG 1분 자료 확보~~ **완료** → 관측소 4소 공간보간까지 적용
2. ~~KIGAM 항공자력 원측선~~ **자료 없음** — 존재하지 않음이 확인되어(2026-08-19)
   이 경로는 닫혔다. 지각층은 현 2.8 km 격자가 상한이고, 복원 벡터의 진폭 부족
   (배율 1.60)은 **설명되지 않는 잔여로 남긴다**. 신규 자력측량은 사업 판단 사항.
3. **잔차 큰 측점 재측정 · 방위표지 고정** — 1단계 판정기준상 남은 조건(현재 13.75′)
4. **2027 신규 캠페인 30점 측정** — DI 플럭스게이트 방위기준 관리 강화.
   민감도상 1차 다항식이 버티는 하한이 14점이라 여유가 2점뿐이다.
5. **NOC 공간투영** — 4소 1차 평면은 근사이고 교란시에 먼저 깨진다

---

## 12-D. 🔒 Midterm Baseline v1 — 중간보고 기준 버전 동결 (2026-08-19)

**다음 주 중간보고까지 모델 실험으로 정본 수치를 바꾸지 않는다.** 지금은 흔들
시점이 아니라 스토리를 완성할 시점이다.

```
Regional        R0 (상수항)
Crustal F       F1 · α = 1
External        E2 (4소 공간보간) · subtract_DI · provisional
Crustal vector  OFF
평가 집합        docs/data/eval_set.json (D 14 · I 15 · F 13 / 16측점)
```

| 성분 | Station-LOSO | 목표 | 상태 |
|---|---|---|---|
| 편각 D | **0.4987° ≈ 29.9′** | 6′ | 목표의 5배 — 구조적 개선 필요 |
| 복각 I | 0.2430° | — | 검증 중 |
| 총자력 F | **63.0 nT** | 50 nT | 목표의 1.3배 — **근접** |

⚠️ 이 값을 바꾸는 변경(모델 구조·자료 필터·External 설정)은 중간보고 뒤로 미룬다.
문서·발표자료·그림 작업만 진행한다.

### 연구 전체 흐름에서의 현 위치

```
① 자료 통합 → ② 4축 시범구축 → ③ 독립검증(현재) → ④ 원인별 개선 → ⑤ 신규 50점 최종검증
```

중간보고의 메시지는 **「미달」이 아니라 「특정」**이다 — 만들었고, 객관적으로 쟀고,
무엇이 성능을 제한하는지 자료·물리·구조로 나눠 지목했다.

### 웹 산출물도 기준 버전에 맞췄다

| 파일 | 손본 것 |
|---|---|
| `lmm.html` | 정확도 고지를 **Midterm Baseline v1** 표기로 교체(구성·LOSO·남은 원인). 지각층에 **결합계수 α** 적용 |
| `lmm_map.html` | `crAlpha()` 로 α 반영 |
| `geomag_globe.html` | 패널에 「중간보고 기준판 v1」 표기 + α 반영 |

⚠️ **α 를 곱하지 않으면 계산기가 조용히 어긋난다** — 모형 정의는
`F = IGRF + R_F + α·A_KIGAM` 이다. 지금은 α=1 이라 값이 같지만, 계수를 바꾸면
클라이언트만 옛 값을 낸다. 세 판본 모두 `crustal.alpha` 를 읽도록 해 두었다.

⚠️ `regTerm()` 은 이미 차수 무관으로 고쳐 두었다(0차 모형에서 NaN 이 되던 문제).
클라이언트를 손볼 때 **차수·계수 개수·α 세 가지**를 항상 함께 확인할 것.

### 발표 본문 — `create_midterm_story_slides.py` (14장)

기존 `create_midterm_lmm_slides.py`(28장)는 검증 기록을 빠짐없이 담은 **자료편**이고,
발표 본문은 흐름 하나로 꿴 **14장**을 따로 만든다. 디자인·도판·수치는 28장 판본의
것을 그대로 import 해 쓰므로 두 판본이 갈라지지 않는다.

| 장 | 내용 |
|---|---|
| Ⅰ 1~2 | 전지구 모델의 한계 · 15점에서 50점으로 |
| Ⅱ 3~5 | 4축 구조 · **자료 활용현황 표** · 구축 파이프라인(0단계 이력감사 포함) |
| Ⅲ 6~7 | 검증체계(구현·구조·예측 + 평가집합 동결) · **모델 선택 결과 3종** |
| Ⅳ 8~9 | 현재 성능 · 확인된 것/남은 것 |
| Ⅴ 10~11 | 편각 원인진단(배제된 가설 6종) · 지각 벡터(개선 확인, 자료 보류) |
| Ⅵ 12 | 2020 연구와의 관계 — 대결이 아니라 확장 |
| Ⅶ 13~14 | 중간결론 3줄 · 후반기 3 Track |

⚠️ **2010 자료는 본문에서 빼고 Appendix 로 돌린다.** 기준시점·환산절차가 불명확해
성능 경쟁 구도로 쓰면 스토리가 흐려진다(12-C 참조).

### 후반기 3 Track

| Track | 목적 | 내용 |
|---|---|---|
| **A 기존자료 고도화** | 편각 30′ 의 원인 분해 | 2012~19 ↔ 2022~25 동일측점 대조 · 장비·방위표지·이력 · 오차 budget · KASA 축 규약 문서 |
| **B 모델 고도화** | KPI 접근 | KIGAM 결측 해결 → 지각 벡터 재판정 · External E2 확정 · 지역장 공간구조 |
| **C 신규 50점** | 개선 자료 + 최종 독립검증 | 35~40점 구축용 · **10~15점 검증 전용** |

**Track C 의 설계가 가장 중요하다** — 50점을 전부 적합에 쓰면 최종 성능을
객관적으로 말할 수 없다. 일부를 처음부터 검증 전용으로 남겨야 최종 보고서에서
「모델 구축에 사용하지 않은 신규 국가기준점에서 독립검증하였다」고 쓸 수 있다.

---

## 12-C. 좌표축·F·2010 대조 (2026-08-19 밤)

### A. KASA 좌표축 규약 — **관측소마다 다르다** (`verify_kasa_axes.py`)

E2 개선폭이 0.8′ 뿐이라 회전각 오차 하나로 뒤집힐 수 있었다. 원자료를 IGRF 와
직접 견줘 확인했다.

| 관측소 | D 관측 | D IGRF | 회전각 | 판정 |
|---|---|---|---|---|
| 청양 | −8.52° | −8.55° | **−0.04°** | 지리축 |
| 이천 | −7.96° | −8.69° | −0.74° | 거의 지리축 |
| 강릉 | −0.88° | −8.99° | **−8.11°** | 계기축 (X≈H, Y≈0) |
| 제주 | −1.20° | −7.53° | −6.33° | **판정 보류** |

⚠️ **제주는 벡터가 닫히지 않는다** — F(벡터) 46,081 vs F(IGRF) 49,173 nT 로
3,092 nT 어긋나고 Z 가 약 5,300 nT 낮다. 일변동 진폭도 이천 대비
**X 0.98 · Y 0.98 · Z 1.30** 이다(`_check_amp.py`). 즉 **Z 감도에 문제**가 있다.

**그럼에도 E2 는 흔들리지 않는다** (평가집합 동결 · degree 0):

| 변형 | LOO D | 뜻 |
|---|---|---|
| E2 회전 있음 | 0.4987° | 채택 |
| **E2 회전 없음** | **0.4997°** | 차이 0.001° — **회전각에 취약하지 않다** |
| E2 제주 제외 | 0.5578° | 남부 앵커가 빠지면 크게 나빠진다 |
| E2 제주 Z 제외 | I 0.2430 → 0.2888 | 감도 오차에도 Z 는 쓰는 편이 낫다 |

편차만 쓰기 때문에 절대 보정 상태가 결과를 좌우하지 않는다. 회전은 사실상
무해하고(청양·이천은 거의 0), 강릉만 실제로 회전이 필요하다.

→ **E2 유지하되 `provisional` 로 표기**한다. KASA 공식 축·baseline 문서를 확보하면
   IGRF 추정 회전을 관측소 baseline orientation 으로 바꾸는 것이 원칙이다.

### B. F 관측시각·센서 위치차 — **야장에 없다** (`audit_fieldbook_F.py`)

2020 절차는 ① D·I 점에서 5 m 이상 떨어져 F 측정 ② 절대측정기 센서 위치에서 재측정
③ 두 위치 차이 보정이다. 야장 68개를 전수로 훑었다.

| 항목 | 결과 |
|---|---|
| 「※ 전자력 측정 결과」 절 | 36건 / 파일 31개 |
| 측정값·기본값 모두 기재 | 30건 |
| 두 값의 차이 | 중앙 **0.56 nT** · 최대 0.99 · **1 nT 초과 0건** |
| **F 측정일시 실기재** | **0건** (전부 00:00) |

**두 값의 차이는 소수점 절사일 뿐이다**(기본값 = 측정값의 정수부). 즉 「기본값」은
두 번째 지점의 측정값이 아니다. **센서 위치차 보정에 필요한 두 지점 F 는 야장에
남아 있지 않고, F 관측시각도 없다.**

→ F 는 세션 전체 시간창으로 보정하는 현재 방식이 **이 자료로 가능한 최선**이다.
   「F 63 → 50 nT」를 이 경로로 얻을 수는 없다. 향후 야장 양식에
   `F_remote / F_DIpoint / 각 측정시각 / 위치차` 칸을 넣도록 건의할 근거가 된다.

### D. 2010 공표 성과 대조 — **LMM 이 과거로 전이되지 않는다** (`benchmark_legacy2010.py`)

`data/'10~'19년 지자기점 관측현황(최종).xls` 의 공표 성과(편각 9점 사용)를
독립 표본으로 썼다.

| 성분 | 표본 | IGRF 잔차 | LMM 잔차 | 개선 |
|---|---|---|---|---|
| 편각 D | 9 | **14.07′** | 24.19′ | **−72 %** |
| 복각 I | 9 | 9.62′ | 18.14′ | −89 % |
| 총자력 F | 9 | 205.8 nT | 198.7 nT | +3.5 % |

**IGRF 단독이 우리 LMM 보다 잘 맞는다.** 원인은 Regional 상수항이다 — 우리
편각 상수는 −12.7′ 인데 2010 표본의 IGRF 잔차 평균은 **+9′** 라 부호가 반대다.

⚠️ 이 대조를 「LMM 이 틀렸다」로 바로 읽으면 안 된다. 공표 성과의 **기준시점이
불명확**하다 — IGRF 와 일치하는 연도를 역산하면 2008~2012 에 걸치고 일부는 그
범위 밖이다(`_check_2010_epoch.py`). 2010 원 관측자료와 당시 청양 자료가 없어
**환산 절차 재현도 불가능**하다.

그러나 방향은 분명하다. **Regional 상수항이 시기 의존적**이라는 것 —
2022~2025 성과에서 얻은 상수를 2010 대 성과에 적용하면 오히려 나빠진다. 이는
그 상수가 정적인 지역장이 아니라 **관측 시기의 계통 오차를 흡수**하고 있을
가능성을 시사한다. 13절의 「측점·시기 고정 계통오차」 진단과 같은 방향이다.

→ 다음 확인 사항: 공표 성과의 기준시점과 환산 절차(2010 보고서 부록),
   그리고 같은 측점의 2010 대 성과와 2022~25 성과를 직접 대조하는 것.

---

## 12-B. v2 검토 반영 (2026-08-19 저녁) — **평가 집합 동결 · External 채택**

### 평가 집합을 얼렸다 (`docs/data/eval_set.json`)

α·R1T·지각 벡터·QC 기준을 모두 **같은 LOO 를 보며** 골랐다. 개발 과정에서는
정상이지만 계속 그러면 LOO 에도 간접 과적합된다. 그래서 기준 설정
(External 미적용 · 지각 벡터 OFF · degree 0 · Grade A)에서 평가 측점을 한 번
정해 파일로 얼렸다 — **D 14 · I 15 · F 13 / 16측점**.

```
python lmm_build.py --freeze-eval    # 의도적으로만 다시 얼린다
```

`loo_cv`·`validate` 는 이 파일이 있으면 그것을 쓴다(`eval_mask`). 적합에는
그때그때의 robust inlier 를 쓰되, **성능을 재는 측점은 고정**된다. 이걸 하기 전과
후의 수치는 비교 대상이 아니다.

### External — E0/E1/E2 비교 후 **E2(4소 평면보간)·성분별 보정 채택**

조건을 모두 맞춰(degree 0 · Grade A · α=1 · 벡터 OFF · 평가집합 고정) end-to-end
로 다시 적합해 견줬다(`compare_external_models.py`):

| 자료원 | subtract_D | subtract_DI | subtract_quiet_DI |
|---|---|---|---|
| 미적용 | 0.5123° | — | — |
| E0 청양 단독 | 0.5495 | 0.5495 | 0.5150 |
| E1 최근접 관측소 | 0.5504 | 0.5504 | 0.5183 |
| **E2 4소 평면보간** | **0.4987** | **0.4987** | 0.5044 |
| E2b 4소 역거리 | 0.5595 | 0.5595 | 0.5384 |

**공간보간만 기준선을 넘는다.** 단일 관측소(E0)·최근접(E1)은 오히려 나쁘다 —
「가장 가까운 관측소와 변화가 같다」는 가정이 한반도 규모에서 성립하지 않는다.
복각도 성분별 구간을 쓰면 0.2505 → 0.2430° 로 함께 좋아져 `subtract_DI` 를 쓴다.

⚠ 개선폭은 작다(D −0.0136°). 12개 설정 중 최소를 고른 것이라 선택편의를 포함하고,
근거는 폭이 아니라 **방식의 타당성**이다.

### External QC 결함 두 가지 수정

| 결함 | 고침 |
|---|---|
| 변동폭을 **X 성분 range 로만** 판정 — Y(편각)·Z(복각) 흔들림을 놓친다 | 관측소 자신의 **D·I·F 시계열 range** 를 각각 재고 ΔB 벡터도 남긴다. 문턱 D·I 3′/8′, F 15/40 nT |
| Kp 를 **세션 시작시각 하나**로만 조회 — 3시간 경계를 넘으면 앞 구간만 붙는다 | 성분 시간창 전체를 30분 간격으로 훑어 **최대 Kp** 사용 |
| 기준선은 평균, 대표값은 중앙값 (규약 불일치) | 기준선도 **중앙값**. `--baseline mean` 으로 민감도 비교 가능 |

수정 후 QC 분포: QUIET 133 · CAUTION 44 · DISTURBED 1 (Kp 기준으로는 107/27/44).

### 지각 결합계수 α — fold 내 재추정으로 확정 (`compare_crustal_scale.py`)

정보 누출 점검이 요점이었다. **fold 마다 훈련 측점만으로 α 를 다시 추정**해도
결론이 같다:

| 모델 | α 전체 | α fold 평균 | α fold 범위 | LOO F |
|---|---|---|---|---|
| F0 지각 미사용 | — | — | — | 72.3 nT |
| **F1 1:1 (채택)** | 1 | 1 | — | **63.0 nT** |
| Fα 추정 | 0.670 | 0.645 | **0.294~0.812** | 67.0 nT |

fold 범위가 0.29~0.81 로 넓다는 것 자체가 α 추정이 불안정하다는 증거다.
시간항 R1T 도 동결 집합에서 D 0.4987 → 0.5459° 로 명확히 악화된다.

### 지각 벡터 강건성 — **이득은 복각이 아니라 편각에 있다** (`test_crustal_vector_robustness.py`)

「복각이 0.2505 → 0.0881° 로 65% 좋아진다」던 관찰은 **평가 집합을 얼리자 사라졌다.**
그 개선은 설정마다 평가 측점이 달라지던 데서 온 것이었다. 네 조건으로 다시 보면:

| 조건 | LOO D | LOO I |
|---|---|---|
| 벡터 OFF (기준) | 0.4987 | 0.2430 |
| ON · zero 채움 | 0.4506 | 0.2397 |
| ON · mean 채움 | 0.4507 | 0.2269 |
| ON · harmonic 채움 | 0.4739 | 0.2434 |
| harmonic · 결측 경계 배제 | 0.4638 | 0.2227 |
| 블록 홀드아웃 남중북 | 0.4848 → **0.4560** | 0.2817 → 0.2264 |
| 블록 홀드아웃 동서 | 0.6619 → **0.6300** | 0.2431 → 0.2280 |

**편각은 모든 채움 방식·경계 배제·두 블록 홀드아웃에서 일관되게 좋아진다**
(0.4987 → 0.45~0.47). 복각은 채움 방식에 따라 오르내려 신호로 보기 어렵다.

→ 그래도 **`CRUSTAL_VECTOR = False` 유지**한다. 편각 이득이 실재하나 채움 방식에
따라 0.45~0.47 로 흔들리고(zero 가 가장 좋고 harmonic 이 가장 보수적), 결측 39%
라는 구조적 결함이 그대로다. 승격 조건은 **KIGAM 결측 자체의 해결**이지 채움
알고리즘 교체가 아니다.

### 측점 이력 정정

| 지적 | 반영 |
|---|---|
| 여주 재설은 **2016년 '재설완료'** | `geometry_version 2 from_year=2016` (2020 가정 폐기) |
| **남해** 누락 | 2020 표지석 유실·좌표 재설정 추가 |
| 2014 거제·조도 **과대오차**, 2015 가야·경주·남지 **망실** | 등급 C 로 추가 |
| `station_version` 과 품질구간을 섞어 쓴다 | **`geometry_version`(물리) / `qc_epoch`(품질)** 분리. 미원·강화는 재설 근거가 없으므로 geometry 는 1 로 두고 qc_epoch 만 나눈다 |

측점 분할(`site_key`, 예 `가야#g1`·`가야#g2`)은 **geometry_version 이 바뀔 때만** 한다.

### 동결 이후의 후보 (더 이상 결과를 보고 규칙을 바꾸지 않는다)

```
Regional        R0 (상수항)
Crustal F       F1 (α = 1)
External        E2 · subtract_DI
Crustal vector  OFF (강건성 통과 전까지)
```

### 현재 정본 (동결 평가 집합 기준)

| 지표 | 값 |
|---|---|
| LOO 편각 D | **0.4987°** |
| LOO 복각 I | 0.2430° |
| LOO 총자력 F | 63.0 nT |

### 남은 항목

- **F 센서 위치차 보정** — 2020 측량은 D·I 점에서 5 m 이상 떨어져 F 를 잰 뒤 위치차를
  보정했다. 야장 원본에 F 측정 위치가 기록돼 있는지 확인 필요
- **legacy 모드**(2010/2020 reduction 재현) 벤치마크
- KIGAM 결측 해결 후 지각 벡터 재판정
- 신규 50점 중 일부를 **validation-only** 로 남겨 최종 KPI 판정

---

## 12-A. LMM 설계안 v2 (2026-08-19) — 2020 연구보고서 반영

2020 지구물리측량 연구보고서를 반영해 파이프라인 순서를 다시 잡았다. 핵심은
**모델 알고리즘보다 순서**다 — 측점 이력 → 정확한 시각 → External reduction →
IGRF 잔차 → 단순 Regional → 독립검증.

```
0. 측점·장비·관측 이력 감사   ← 이번에 신설된 단계
1. 시간대 UTC 통일
2. 세션별 관측 품질 QC
3. External reduction (CYG·KASA)
4. 실제 관측시점 IGRF
5. LMM 적합 (Regional + Crustal)
6. Station-LOSO 검증
7. 독립 신규측점 검증
```

### 0단계 — 측점 이력 감사 (`data/station_history.csv`)

**같은 이름이라고 같은 측점이 아니다.** 표지 재설·방위표지 변경·좌표 변경이 있으면
다른 station version 이고, 과거·현재 잔차를 한 점으로 묶으면 안 된다.

| 등급 | 뜻 | 처리 |
|---|---|---|
| A | 야장·시각·표지·장비 정상 | 적합에 사용 |
| B | 메타데이터 일부 불완전 | 검증·비교용 (적합 제외) |
| C | 이설·망실·장비 고장·방위각 이상 | 제외 |

2020 보고서에서 옮겨 온 판정: **2017 전량 제외**(편복각경위계 고장) · **2011 D·I
미관측** · 여주/가야/순창 **표석 망실 후 재설** · 미원/강화 **편각 이상** ·
와도/화천 **환경 변화**. 2020 신규측량의 가야 편각 약 1° 차이는 청양 장비 비교
(0.12° 차)로 **장비 비정상 동작이 주원인**으로 판단됐다.

여기에 2026-08 재방문 감사(부안 2023·포천 2023 불일치)를 합쳤다.

⚠️ **재설 연도가 보고서 본문에 없어 2020 년을 경계로 가정**했다. 부록에서
확인되면 CSV 의 `from_year`/`to_year` 만 고치면 된다.

현재 표본에서 판이 갈리는 것은 **미원뿐**(2019 v1 = B 제외 · 2022~24 v2 = A).
`site_key` 로 `미원#v1`·`미원#v2` 를 분리해 집계·LOSO 한다.

### 지각 결합계수 α — 검정 결과 α=1

「α=1 을 진리로 놓지 말라」는 지적을 실제로 검정했다(평가 측점 고정 LOO F):

| 모델 | α | LOO F |
|---|---|---|
| F0 지각 미사용 | 0 | 72.3 nT |
| **F1 1:1 결합 (채택)** | **1** | **63.0 nT** |
| Fα 결합계수 추정 | 0.670 | 67.0 nT |

**α 를 자유롭게 두면 F inlier 13점에 과적합한다.** 표본 내 적합은 좋아지지만
예측은 나빠진다. α=1 이 답이었다 — 다만 그것을 **검정으로 확인**한 것이 요점이다.

### 시간항 R1T — 기각

`R = a + bφ + cλ + d(t−t0)` 를 검정했다. 편각 0.5022 → 0.4995°(무의미),
복각 0.0881 → 0.0971°(악화). **16측점으로는 지역 영년변화 항을 지지할 수 없다.**

### 지각 벡터의 D·I 적용 — 진단 전용으로 격하 (`CRUSTAL_VECTOR = False`)

결측 39% 를 0 nT 로 채운 FFT 역산이라 생산 모형에 넣지 않는다(설계안 v2 12항).

⚠️ **성능만 보면 켜는 쪽이 낫다** — 평가 측점 고정 비교에서 편각 0.5123 → 0.5022°,
**복각 0.1972 → 0.0881°**. 끄는 것은 성능이 아니라 **신뢰성 판단**이다. 결측 보간을
0 채움에서 조화 내삽으로 바꾼 뒤 다시 판정할 것. 스위치는 상수 하나다.

### External QC — Kp 는 기준이 아니라 플래그

Kp 는 전지구 지수이고 CYG·KASA 는 한반도에서 직접 잰 값이다. 그래서 **Kp>2 를
제외 기준으로 쓰지 않고**, 그 시간창에서 관측소가 실제로 흔들렸는지(변동폭)로
판정한다(`lmm_external_multi.session_qc`).

| | QUIET | CAUTION | DISTURBED |
|---|---|---|---|
| Kp 기준 | 99건 | 30건 | 38건 |
| **관측소 변동폭 기준** | **126건** | 37건 | **4건** |

→ **Kp 로 교란이라 본 세션 대부분이 실제로는 조용했다.** 세션 대표값도 평균에서
**중앙값**으로 바꿨다(1분 자료의 스파이크에 평균이 끌려간다).

### 남은 항목

- **E0/E1/E2 비교** (청양 단독 / 최근접 / 공간보간) — `--mode` 로 산출은 되나
  LOSO 비교 미실시. External 이 현재 미채택이라 우선순위 낮음
- **legacy 모드**(2010/2020 방식 재현) — 국가성과 처리방식과의 차이 제시용
- **F 센서 위치차 보정** — 야장 원본에 F 측정 위치가 기록돼 있는지 확인 필요
- **결측 보간 개선 후 지각 벡터 재판정**

---

## 13. 편각 잔차 원인 진단 (2026-07 수행 · 2026-08-19 외부검토 반영)

> **외부검토 반영 요지** — 분석 흐름은 유지하되 확정 표현을 근거 수준으로 낮췄다.
> ① 지각 벡터 예상 기여(7.8′)는 **KPI 6′ 를 넘으므로 「탈락」이 아니다** — 현재
>   주원인이 아닐 뿐, 자료 계통 오차를 걷어낸 뒤의 **2차 오차원**으로 남긴다.
> ② 반변량도의 **「순수 nugget」 확정은 철회** — 최근거리 구간 측점쌍 5개, 거리 추세
>   p 값도 91개 쌍을 독립으로 본 값이라 과소평가돼 있다. 「측점을 늘려도 소용없다」는
>   문장은 삭제했다(오히려 늘려야 판정된다).
> ③ **D/I = F/H 는 등방 가정 전용** — 「편각에만 30′ 초과 오차 존재」는 단정하지 않는다.
> ④ 시기간 IGRF 잔차 차는 **정확히 0 일 필요 없다** — 「작은 값으로 수렴해야」로 표현.
> ⑤ **격자 간격은 진폭 상한이 아니라 파장 한계** — 「2.8 km 가 182 nT 를 담을 수 없다」는
>   틀린 표현이다. 「단파장 국소 이상을 복원할 수 없어 존재 여부를 판단하기 어렵다」.
> ⑥ **결측 39% zero-fill FFT 가 가설 A 검증의 최대 약점** — 3.2절은 **탐색적 분석**으로
>   격을 낮췄다.
> ⑦ 향후 순서 마지막에 **「지각 벡터의 KPI 관점 재평가」 단계를 추가**했다.


`lmm_diagnose.py` → `docs/data/lmm_diagnosis.json` → `create_lmm_validation_report.py`

### 확정된 사실

| 항목 | 결과 |
|---|---|
| ① Core 층 구현 | 공식 IGRF 계산기와 **표시 자릿수 내 일치** (ppigrf ↔ lmm.html 0.2″ / 0.07 nT) |
| LMM 계산 구현 | Python 독립 재현과 0.3″ / 0.1 nT 이내 일치 |
| ~~지각층은 D·I 에 기여하지 않음~~ | **2026-08-19 해소** — 스칼라에서 벡터를 복원해 D·I 에도 기여시켰다(아래 「지각 벡터 성분」 절) |
| 배포 격자 양자화 | `lmm_model.json` 의 crustal 값은 **정수 반올림** — 원 격자와 약 0.2 nT 차 |

### 편각 잔차 원인 — 네 가설 모두 기각

편각 잔차 RMS **35.0′**(inlier 14점) 에 대해:

| 가설 | r | p | 판정 근거 |
|---|---|---|---|
| A. 지각 벡터 성분 누락 | +0.244 | 0.400 | 복원 벡터 예측 **8.1′** — 주원인은 아니나 **KPI 6′ 를 넘어 무시 불가** |
| B. 측정 정밀도 | +0.131 | 0.701 | 1회 관측 29.0′ vs 2회 이상 28.4′ 로 동일 |
| C. 국소 자기 거칠기 | +0.187 | 0.541 | — |
| D. 미적합 지역 구조 | — | — | 반변량도에 공간 상관 **확인 안 됨**. ⚠ 최근거리 구간 측점쌍 5개뿐 — nugget 확정 근거로 쓰지 말 것 |

⚠️ **가설 B 의 검정 대상은 정밀도(precision)이므로 그 기각이 계통 오차(accuracy)를
배제하지 않는다.** 오히려 **성산**이 결정적 — 2년 간격 독립 캠페인 두 번이 0.6′ 로
일치하면서 둘 다 34′ 틀렸다. 측점에 고정된 계통 오차의 서명이다.

**D/I 비대칭**이 원인을 좁힌다 — 관측 비 **3.69** vs 자기 원인 예상 1.64(=F/H).
⚠️ 이 기대값은 δX·δY·δZ 가 비슷한 분산을 갖는 **등방 자기이상 가정에서만** 성립하므로,
비가 크다는 것으로 초과 오차의 **크기를 특정하지 말 것**. 말할 수 있는 것은 편각 잔차가
복각보다 현저히 크다는 사실과, 그것이 편각 측정·방위 기준 계통 오차를 시사한다는 데까지다.
후보는 ① 마크 진북 방위각 오차,
② 성과표 편각 전사·환산 오류, ③ 편각 환산 절차 오류. **자료만으로 구분 불가.**

배제된 것: **좌표 오류**(잔차 설명에 174 km 필요, 미원 353 m 는 0.055′ 로 무의미),
기준시점 환산 오류(D 기울기 −5.18′/년인데 I 는 +0.38′/년로 짝이 안 맞음).

### CYG 는 보유·사용 중 — 막힌 것은 성과표

`data/cyg/` 실측: quasi-def 2019-05-04~11-01 **78일**(야장 보정에 사용),
2024-06-10~19 **10일**(일변동 규모 측정용). 적용 불가 구간은 2022~25 뿐이고 원인은
[lmm_build.py:82] `df["date"] = dt.datetime(int(y), 7, 1)` — **연도만 알고 7월 1일로
대체**한다. 시각이 없어 1분 자료를 조회할 수 없고, 날짜조차 몰라 다운로드도 못 한다.

| 영향 | 규모 |
|---|---|
| 영년변화(Core) 오차 | ±0.7′ — 무시 가능 |
| External 층 | **봉쇄** (편각 6.7′ 중앙 ~ 13.2′ 최대) |

계산기의 「④ External 미적용」 표시는 **정상**이다. LMM 은 정온시 기준값 모형이므로
외부장 보정이 들어갈 자리는 예측 시점이 아니라 **관측 성과 정리 단계**다.

### External 성분별 적용 (2026-08-19) — 편각만 보정

감사로 편각 잔차가 35→20.7′ 로 줄자 **같은 보정이 성분별로 다르게 작동**했다.

| 성분 | 보정량 | 잔차 | 비중 | 결과 |
|---|---|---|---|---|
| D | 5.4′ | 20.7′ | 26% | LOO 0.5675 → **0.5403°** 개선 |
| F | 38 nT | 약 190 nT | 20% | LOO 58.41 → 66.74 nT **악화** |

F 가 악화되는 이유는 잔차의 **성격**이 다르기 때문이다. F 잔차는 외부장이 아니라
**지각장 불일치**가 지배하므로, 공간적으로 균일한 시간 보정을 빼면 잡음만 더해진다.
편각 잔차는 방위 기준 오류를 걷어낸 뒤라 외부장 몫이 상대적으로 커졌다.

→ `EXTERNAL_MODE = "subtract_D"` — **편각에만 적용**한다. 감사 전 표본에서는 D 도
악화됐었다(+0.079°); 방위 기준 오류가 외부장 신호를 덮고 있었기 때문이다.
**즉 External 의 효용은 Regional 품질에 종속된다** — 감사를 먼저 한 것이 옳았다.

**오늘 누적 개선** — LOO 편각 0.7691 → **0.5403°**(−30%), 편각 잔차 RMS 35.0 → **17.31′**
(−51%), 가설 A(지각 벡터) r=+0.244 p=0.40(기각) → **r=+0.625 p=0.040**(유의).
복원 벡터 예측 8.33′ 가 잔차 17.31′ 의 **48%** 를 차지한다.

### ⚠️ 순환 적합 발견과 재판정 (2026-08-19 오후) — 이 절을 먼저 읽을 것

지구본의 편각 패치가 **IGRF 와 반대 방향으로 기울어** 있다는 지적에서 출발해
파이프라인의 순환을 찾았다. **오전에 기록한 개선 수치들은 상당 부분 이 순환의
산물이며, 아래 값이 정본이다.**

**무엇이 순환이었나** — `robust_lstsq` 는 「그 차수의 적합면에 잘 맞는 점」을 남긴다.
inlier 를 1차 적합으로 고르면 **평면 기울기에 동조하는 점만 살아남아 그 기울기를
스스로 정당화**한다. 실제로 편각에서:

| 표본 | 경도와 잔차의 상관 |
|---|---|
| 1차 적합이 고른 inlier 11점 | r = **+0.674** |
| 전체 16점 | r = +0.255 (p = 0.34, **유의하지 않음**) |

그 결과 Regional 편각에 **+0.32°/°경도** 의 기울기가 생겼다 — IGRF 실제 기울기
(−0.077°/°경도)의 **4배이자 반대 부호**이고, 한반도 폭으로 환산하면 1.45° 다.
복각·총자력에는 같은 기울기가 없어(r=−0.07, +0.06) 자기장 원인으로 볼 수 없다.

⚠ **자오선 수차(grid convergence) 가설은 기각됐다** — γ=(λ−λ₀)·sinφ 와의 상관은
통일원점 +0.25(p=0.35) · 권역별 −0.42 이고, 1:1 차감하면 잔차가 30.5→40.9′ 로
악화된다. 격자북/진북 혼동이 아니다.

**조치** — `REGIONAL_INLIER_DEGREE = 0`. inlier 판정을 **기울기를 만들 수 없는
0차 적합**으로 고정해 되먹임을 끊는다(`fit_regional` 의 `A_sel`).

**재판정 결과 — 평가 측점을 「지각벡터 미적용·External 미적용·0차」에서 한 번만
정해 고정하고 전 설정을 같은 측점에서 견줬다** (`_fair_grid.py`):

| 지각벡터 | External | 자료원 | LOO D | LOO I |
|---|---|---|---|---|
| F만 | none | — | 0.5118° | 0.2499° |
| **적용** | **none** | — | **0.5016°** | **0.2201°** | ← 채택 |
| 적용 | subtract_D | cyg | 0.5098° | 0.2201° |
| 적용 | subtract_D | multi | 0.5177° | 0.2201° |
| 적용 | subtract_quiet_D | cyg | 0.5189° | 0.2201° |
| 적용 | subtract_quiet_D | multi | 0.5323° | 0.2201° |

1. **지각 벡터는 유지** — 편각 −0.010°, 복각 −0.030°. 개선폭은 작지만 두 성분이
   같은 방향이고, 물리적 근거(스칼라→벡터 역산)가 있다.
2. **External 은 「none」으로 되돌린다** — 어떤 보정도 기준선을 넘지 못한다.
   보정량(편각 5.4′)이 잔차(26′)에 견줘 작아 16측점 표본에서는 가려진다.
   ⚠ 오전의 「4소·정온 적용으로 0.365→0.323°」는 **순환 비교였다.**
3. **Regional 차수는 0차(상수항)** — 차수 선정도 같은 순환에 걸려 있었다.
   순환을 끊으면 0차 0.5016 / 1차 0.5691 / 2차 0.6330 으로 **공간항이 필요 없다.**
   즉 **16측점으로는 지역장의 공간 구조를 결정할 수 없다** — 측점 확충의 직접 근거.

**정본 수치 (2026-08-19 최종)**

| 지표 | 값 | 비고 |
|---|---|---|
| LOO 편각 D | **0.5123°** | 목표 0.1° 미달 |
| LOO 복각 I | 0.2505° | 지각 벡터를 끄면서 악화(0.0881→) |
| LOO 총자력 F | 63.0 nT | 목표 50 nT 미달 |
| 편각 잔차 RMS | 29.3′ | inlier 14 |
| Regional | 상수항만 | 공간항·시간항 미채택 |
| Crustal 결합 | α = 1 (1:1) | α 추정은 과적합으로 기각 |
| 가설 A (지각 벡터) | r=+0.478 p=0.084 | 경계 — 진단 전용 |

⚠️ 이 표는 **설계안 v2 반영 후**의 값이다. 직전(지각 벡터 켬·이력감사 전)은
D 0.5016 · I 0.0876 · F 61.4 였다.

⚠️ **교훈 — 강건 적합의 inlier 로 성능을 재지 말 것.** 설정을 바꿔 가며 비교할
때는 **평가 집합을 한 번만 정해 고정**해야 한다. 이 프로젝트의 LOO 는 오랫동안
inlier 기준으로 보고돼 왔으므로, 과거 기록의 절대값은 이 기준으로 다시 읽어야 한다.

⚠️ **클라이언트 주의** — `regTerm()` 이 1차 고정이면 0차 모형에서 `c[1]` 이
undefined 가 돼 **전체가 NaN 으로 무너진다**(지구본 D/I/F 가 null 로 나왔다).
`lmm_map.html`·`geomag_globe.html` 의 `regTerm` 은 계수 개수만큼만 항을 만든다.
`lmm.html` 은 원래 `polyTerms(deg)` 라 영향 없었다.

---

### 지각 벡터 성분 (2026-08-19) — `lmm_build.crustal_vector`

**종전에 지각층은 F 에만 더해졌다**(`lmm.html:295` 의 설계 선택). 항공자력이
스칼라 이상만 주기 때문인데, 이는 자료의 한계가 아니라 **쓰지 않았을 뿐**이다.

총자력 이상은 이상벡터를 주자기장 방향으로 투영한 값이다:

```
ΔF = l·b_north + m·b_east + n·b_down      (l,m,n) = 주자기장 단위벡터
```

포텐셜장은 조화함수라 파수영역에서 이 관계가 닫힌다. 역산하면 3성분을 되찾는다.
재투영 검증에서 오차는 **상수 오프셋뿐**(중앙 +14.98 nT · 표준편차 0.0000)이고,
그 상수는 파수 0 성분이 복원되지 않아 생기며 Regional 상수항이 흡수한다.

| 지표 | 지각 F만 | **지각 벡터** | 비고 |
|---|---|---|---|
| LOO D (기준 inlier 고정) | 0.5403° | **0.3396°** | −37%, 공정 비교 |
| LOO I (기준 inlier 고정) | 0.2600° | **0.2408°** | −7% |
| LOO D (자유 판정) | 0.5403° | 0.3653° | inlier 11→12 |
| LOO I (자유 판정) | 0.2600° | 0.0429° | ⚠ inlier 15→**8**, 선택효과 |
| 전 16점 평가 | D 0.6533° | **0.5293°** | 배제 없이도 개선 |
| 가설 A (지각 벡터) | r=+0.244 p=0.40 | **r=+0.657 p=0.020** | 유의 |
| 적용 후 잔여 상관 | — | r=+0.31 p=0.32 | 이중계상 없음 |

**측점 평균 기여는 D 7.4′ · I 6.6′** 이고 편각 잔차 13.75′ 의 61% 를 예측한다.
최적배율이 **1.60** 이라는 것은 **격자 평활로 진폭이 과소평가**된다는 뜻이다.
⚠️ 다만 **원측선 자료는 존재하지 않는다**(2026-08-19 확인). 이 진폭 부족분은 현
자료로 채울 수 없고, 신규 자력측량 여부가 갈림길이다 — 산출물에 「원측선을 확보하면
된다」로 쓰지 말 것.

| 구현 위치 | 내용 |
|---|---|
| `lmm_build.crustal_vector` | FFT 역산 (`scipy.signal.windows.tukey` 창 + 반사 패딩) |
| `lmm_build.CrustalVector` | 쌍선형 보간자 → (b_north, b_east, b_down) |
| `lmm_build.attach_crustal_di` | 측점표에 `crD`·`crI` 열 부착 (없으면 0 = 종전 동작) |
| `lmm_model.json` `crustal.vector` | **b_north·b_east 두 격자만** 싣고 하향은 유도 |
| `lmm.html` `crustalVec()` | 쌍선형. `lmm_map.html`·`geomag_globe.html` 은 최근접 |
| `lmm_excel.py` `CRUSTAL_VEC` 시트 | 두 격자 + `ATAN2` 로 D·I 기여 |

⚠️ **b_down 은 내보내지 않고 유도한다** — `b_down = (ΔF + dc − l·b_north − m·b_east)/n`.
용량을 한 격자 아끼는 것보다 **스칼라 F 층과 벡터가 정의상 어긋날 수 없다**는 것이
중요하다. 정수 양자화 후 유도 오차는 RMS 0.36 nT (I 로 환산 0.02′).

⚠️ `lmm_diagnose.py` 는 **같은 역산 함수를 호출**한다(사본을 두면 조용히 갈라진다).
진단의 `rD`·`rI` 는 이제 「지각 벡터까지 뺀」 잔차이고, 가설 A 검정은 벡터를 빼기
전 잔차(`rD_novec`)로 한다 — 모형이 이미 뺀 것을 다시 상관 보면 0 이 나온다.

---

### ⚠️ 「2022~25 는 관측시각이 없다」는 서술은 폐기 (2026-08-19)

야장 원본을 확보한 뒤에도 옛 서술이 검증기록 4장에 남아 있었다. **사실이 아니다.**
`docs/data/fieldbook_sessions.csv` 실측:

| 연도 | 2019 | 2020 | 2022 | 2023 | 2024 | 2025 |
|---|---|---|---|---|---|---|
| 세션 | 23 | 39 | 13 | 50 | 43 | 44 |
| 시각 확보 | 22 | 39 | **13** | **50** | **43** | **44** |

시각 결측은 **성산 2019-09-18 한 건뿐**이다. 「자료가 없어 봉쇄」가 아니라
**있는 자료를 쓰지 않았던 것**이고, 지금은 쓴다(`attach_fieldbook_datetime`).

**성분별 시간창** — 야장은 세션 전체(`시작`~`종료`) 외에 **`편각구간`·`복각구간`을
따로** 기록한다. 두 구간의 중심시각 차이는 **중앙 12분 · 90백분위 18분 · 최대 53분**
(30분 초과 2세션)이므로, D 는 편각 구간, I 는 복각 구간의 외부장으로 각각 보정한다
(`lmm_external_multi.parse_window`). F 는 야장 기록이 일자 단위라 세션 전체를 쓴다.
같은 세션에서 D·I 보정량이 갈리는 폭은 중앙 2.33′ 다.

⚠ 성분별 창을 적용해도 **모형 반영 판정은 바뀌지 않는다** — 고정 평가 집합에서
none 0.5016 / subtract_D·multi 0.5156 / subtract_DI·multi 0.5156(I 는 0.2201→0.2357 악화).
**보정량은 산출·보관하되 반영하지 않는다.** 반영을 막는 것은 시각이 아니라 **규모**다
(보정량 6.8′ vs 잔차 26′).

---

### External — 관측소 4소 공간보간 (2026-08-19) — `lmm_external_multi.py`

자문 권고(대체관측소 활용)를 **모형에 실제로 반영**했다. 청양 단독 균일 V 근사
대신 **청양·제주·강릉·이천 4소**의 세션 편차를 1차 평면으로 공간보간한다.

| 요점 | 내용 |
|---|---|
| 기준선 | 관측일 ±10일 · Kp≤2 · 지방시 23~03시 평균 (`lmm_external` 규약과 동일) |
| 보간 | `plane`(3계수, 3소 이상) → 미달 시 `idw`(p=2) 자동 강등. `nearest` 는 대조군 |
| 환산 | 편차를 **측점의 IGRF 기준장**에 얹어 D·F 로 환산 (관측소 기준장을 쓰던 종전보다 정확) |
| 산출 | `docs/data/external_corrections_multi.csv` — 178/211 세션 (4소 130 · 3소 42 · 2소 6) |

⚠️ **계기 좌표계 회전 정합이 필수다.** 강릉·제주 Y 는 변화계 기준(Y≈0)이라
편차 벡터를 측점 좌표계로 옮기려면 회전을 맞춰야 한다.
`α = D_IGRF(관측소) − median D_관측` 으로 구한 실측 회전각:
**제주 −6.32° · 강릉 −8.10° · 이천 −0.69° · 청양 −0.03°** —
「이천은 지리 정북, 강릉·제주는 계기 기준」이라던 종전 기록과 정확히 맞는다.

**end-to-end 재적합 (생산 설정 · degree 1 · 16측점)**

| 모드 | 자료원 | LOO D | LOO F | 판정 |
|---|---|---|---|---|
| none | — | 0.5899° | 58.41 nT | 기준선 |
| subtract_D | cyg | 0.3653° | 58.41 | 종전 채택 |
| subtract_D | multi | 0.4028° | 58.41 | **단독 적용은 오히려 나쁨** |
| subtract_quiet_D | cyg | 0.3763° | 58.41 | |
| **subtract_quiet_D** | **multi** | **0.3232°** | **58.41** | **채택** |

**다중 관측소는 정온 세션에 한정할 때만 이득이 난다.** 교란시에는 4소 평면적합이
크게 발산해(|dF| 최대 567 nT) 잡음을 키운다 — **공간투영 근사가 폭풍 중에 먼저
깨진다**는 뜻이고, 이는 NOC 모드 분해가 필요한 이유와 같은 방향이다.

⚠️ **8개 설정 중 LOO 최소를 고른 것이라 0.365→0.323 은 선택편의를 포함한다.**
근거로 삼을 것은 차이의 크기가 아니라 **방향(정온 한정 · 공간보간)이 물리적으로
타당하고 자문 권고와 일치한다**는 점이다.

---

### 자기이상 구배 — 필터에서 가중치로 (2026-08-19)

`geomag_site_selection.py`. 종전은 계단 점수(5/8/10/7) + **800 nT 하드 제외**였다.
계단은 경계값 1 nT 차이로 3점이 뛰고, 하드 제외는 「고구배 = 부적합」 판단을
후보 생성 단계에 못 박아 뒤에서 되돌릴 수 없게 한다.

```
s5 = 10 · exp(−½ · (ln(v/250) / σ)²)      σ = 1.80(저구배쪽) / 0.55(고구배쪽)
```

| 구배 | 10 | 30 | 80 | 150 | **250** | 400 | 600 | 800 | 1200 nT |
|---|---|---|---|---|---|---|---|---|---|
| 점수 | 4.0 | 5.0 | 8.2 | 9.6 | **10.0** | 6.9 | 2.8 | 1.1 | 0.2 |

저구배쪽에만 하한 4.0 을 둔다 — 기여도는 낮아도 **측정 환경은 조용하다**는
종전 취지를 살리기 위해서다. 고구배쪽에는 하한을 두지 않는다(인공잡음 위험이
실제 감점 사유이므로). `ANOMALY_GRADIENT_MODE="filter"` 로 종전 동작 복귀 가능.

⚠️ 하드 제외를 풀었으므로 **`zones["anomaly"]` 가 None 이 된다** — 후보 생성
단계에서 잘라내지 않고 점수로만 반영하며, 배제 여부는 현장 정밀 자력측량에
넘긴다. 지도의 등급별 레이어(`gradient_*.geojson`)는 표시용이라 그대로다.

**재실행 결과 (2026-08-19)** — 후보 **178개가 하나도 바뀌지 않았다**.
800 nT 초과 셀 20개에 애초에 후보가 없었기 때문이다. 즉 **하드 제외는 실제로
아무것도 걸러내지 않고 있었다.** 바뀐 것은 점수뿐이다:

| | 이전(계단) | 이후(가중치) |
|---|---|---|
| s5 고유값 | 6개 (0/5/6/7/8/10) | 연속 |
| s5 평균 | 7.66 | 7.27 |
| 등급 분포 | P1 63 · P2 56 · P3 59 | P1 64 · P2 57 · P3 57 |

→ 자문 권고의 실익은 「후보를 되살리는 것」이 아니라 **경계값 불연속을 없애고
배제 판단을 현장으로 넘기는 것**이었음이 결과로 확인됐다.

---

### 자문의견 대조 (2026-07-29 자문회의) — 반영 현황

| 자문 권고 | 현황 |
|---|---|
| 일변화 미보정이 근본 원인 | ✅ 독립 확인 일치. 성과표 원시값 확정 + External 편각 적용 |
| 청양 단독 차감 한계(거리·위도) | ✅ 실측 확인. 균일 V 근사가 F 를 악화시킴 → 편각에만 적용 |
| 2020~25 야장·시각 확보 | ✅ 완료 (212세션) |
| Kp 반영 | ✅ 정온 판정·교란 배제 모드 구현 |
| 기존점 저수지 재검토 | ✅ `EXISTING_TARGET` — 기존 30점 중 15점(저수지·제방 15점 제외) |
| **2010 vs 2020~24 에폭 혼용 금지** | ⚠ **부분 위배** — 아래 참조 |
| **측점 수 대비 자유도 민감도 분석** | ✅ 아래 수행 (8월 말 재자문 요구 항목) |
| 구배를 필터가 아닌 **가중치**로 | ✅ 2026-08-19 구현 — 로그축 비대칭 곡선 + 하드 제외 해제 |
| 대체관측소(이천·제주·소백산) 활용 | ✅ 2026-08-19 구현 — 4소 평면 공간보간(`lmm_external_multi.py`) |
| 그레디오미터 도입 | — 장비 사안(연구 범위 밖) |

**① 측점 수 대비 자유도 민감도** — 무작위 부분표본 12회 중앙값(생산 설정)

`lmm_sensitivity.py` (무작위 부분표본 12회 중앙값, 생산 설정 — 2026-08-19 재실행)

| 측점 | 1차(3계수) LOO D | 2차(6계수) LOO D | 1차 F | 2차 F |
|---|---|---|---|---|
| 16 | **0.3232°** | 0.8824° | 58.4 nT | 64.7 |
| 14 | 0.3378° | 1.0618° | 58.4 | 89.5 |
| **12** | **0.7181°** | 1.2809° | 69.9 | 253.4 |
| 10 | 0.4768° | 2.3502° | 72.1 | 322.3 |
| 8 | 0.8465° | — (자유도 부족) | 116.2 | — |

→ 「15점 최소 구성」 물음의 답: **1차 다항식이면 14점까지 버티고 12점에서 무너진다.**
(종전 판정은 12점까지였다 — 모델이 좋아지면서 요구 표본도 올라갔다.)
2차는 16점에서도 1차보다 나쁘므로 **현재 측점 수로는 2차를 쓸 수 없다.**

⚠ 10점이 12점보다 좋게 나오는 것은 **부분표본 추출 잡음**이다(12회 중앙값이라도
어느 측점이 빠지느냐가 지배한다). 값의 순서가 아니라 **12점 이하에서 흔들리기
시작한다**는 사실만 읽을 것.

**② 에폭 혼용** — 자문은 2020~24 로 모델하고 2010 은 분리하라고 권고했다. 우리는
2019 야장을 포함 중이다(IGRF-13 에폭).

| | 측점 | 기간 | LOO D | LOO F |
|---|---|---|---|---|
| 2019 포함 (현행) | 16 | 2019~2025 | 0.5403° | **58.41 nT** |
| 2019 제외 | 14 | 2022~2025 | **0.5051°** | 62.26 nT |

**편각은 2019 를 빼는 쪽이 낫고(−0.035°) 총자력은 넣는 쪽이 낫다(−3.9 nT).**
자문 권고 방향과 편각 결과가 일치한다. 다만 측점이 14 로 줄어 위 ① 의 자유도 여유가
깎이므로, **정식 편각 산출 단계에서는 2019 제외를 채택**하고 시범 단계에서는 포함해
표본을 유지하는 절충이 합리적이다. `INCLUDE_2019` 로 전환한다.

---

### 신규 선점 배치 계획 — `plan_new_sites.py`

측점 부족은 개수만이 아니라 **배치**의 문제다. 감사로 부안이 빠지면서 서해안이 비었다.
국토 격자(0.1°, 1,088점)에서 최근접 측점 거리를 재고, 103개 후보 중 **최대 공백을 가장
크게 줄이는 순서**로 탐욕 선택한다.

| 현재 (16측점) | 값 |
|---|---|
| 최근접 측점 거리 중앙 | 41 km |
| 90 백분위 | 81 km |
| **최대 공백** | **129 km** (38.60°N 128.40°E, 강원 북동) |
| 50 km 초과 지역 | 37.6% |

상위 5개: **현리(C) · 보령(D) · 부안(B) · 화천(A) · 조도(C)**. 20개를 모두 넣으면
최대 공백 **129 → 52 km**, 평균 45 → 25 km.

⚠️ **배치 최적과 자기환경 등급은 다른 축이다** — 상위 20 중 A·B 는 11개뿐이고,
2위 보령은 등급 D(부적합)다. 공백이 큰 곳일수록 후보가 적어 등급이 낮은 경향이 있다.
**두 축을 함께 놓고 정해야 한다** — 배치가 급하면 D 등급 지역은 대체지 재탐색이 필요하다.

---

### Regional 성과 신뢰도 감사 (2026-08-19) — `audit_regional_reliability.py`

13절 1단계의 본론. 야장 원시 판독값 197세션(18측점 · 2019~2025)을 세 층위로 검정했다.

| 층위 | 결과 | 판정 |
|---|---|---|
| ① 내부 산술 | 편각 재계산차 중앙 **0.16″** · 최대 8.6″ | **완전 일치** |
| ② 정밀도 (같은 날 세션 산포) | 중앙 **1.41′** · 42일 중 6′ 초과 2일 | **양호** (KPI 6′) |
| ③ 정확도 (재방문 잔여) | RMS **33.7′** · 16구간 중 **9구간이 6′ 초과** | **불량** |

**③ 이 결론이다 — 정밀하지만 부정확하다.** 재방문 잔여는 「관측 ΔD − IGRF 영년변화」로,
같은 표석을 다시 재면 0 이어야 한다. RMS 33.7′ 는 13절에서 관찰된 **편각 35′ 미설명
잔차와 같은 크기**다. 즉 그 잔차의 정체가 재방문 불일치였다.

| 판정 | 구간 |
|---|---|
| 정상 (<6′) | 미원 2.0 · 상주 −5.2/2.5 · 성산 −0.6 · 순천 −1.5/0.5 · 함양 −5.8 |
| 주의 (6~20′) | 거제 −19.7 · 여주 11.0 · 이원 −8.3 |
| **불일치 (≥20′)** | **부안 62.2 / −70.6 · 함양 76.9 · 포천 −32.2 / 22.3 · 가야 −35.5** |

**혐의 방문 특정** — 연속 구간의 잔여 부호가 뒤집히면 가운데 방문이 원인이다:
**부안 2023**(+62.2 → −70.6) · **포천 2023**(−32.2 → +22.3). 가야는 2회 방문뿐이라
어느 쪽이 틀렸는지 못 가린다.

⚠️ **마크 방위각이 재방문마다 바뀐다** — 같은 측점인데 참방위각이 거제 159.9°→330.6°,
포천 33.2°→192.7°, 부안 54.4°→169.6°→177.5° 로 옮겨간다(다른 표지를 시준했거나 정·반
시준). 16개 구간 중 **60″ 초과 변동이 16건 전부**. 마크를 바꾸는 것 자체는 오류가 아니나,
바뀐 마크의 참방위각이 틀리면 그 방문의 편각이 통째로 그만큼 틀어진다 —
③ 의 불일치와 정확히 맞물리는 구조다.

**감사 반영 재적합 (`REGIONAL_FILTER = "strict"`)** — 원인이 특정된 2건(부안 2023 ·
포천 2023)을 빼고 처음부터 다시 적합했다. **감사는 모델 잔차를 보지 않고 야장 원시
판독값과 IGRF 영년변화만으로 지목했으므로 순환 논증이 아니다.**

| 지표 | 감사 전 (17측점) | 감사 후 (16측점) | 변화 |
|---|---|---|---|
| LOO 편각 D | 0.7691° | **0.5675°** | **−26%** |
| LOO 총자력 F | 60.26 nT | **58.41 nT** | −1.85 |
| LOO 복각 I | 0.1787° | 0.2600° | +0.081 ⚠ |
| 편각 잔차 RMS | 35.0′ | **20.73′** | **−41%** |
| **D / I 비대칭** | **3.69** | **1.95** | 자기 원인 예상 1.64 에 근접 |
| 가설 A (지각 벡터) | r=+0.244 p=0.40 | **r=+0.560 p=0.046** | 기각 → **유의** |

⚠️ **13절의 진단이 바뀐다.** 종전에 「편각에만 작용하는 초과 오차 약 30′」로 적었던 것의
정체가 **그 두 방문**이었다. 걷어내니 D/I 비대칭이 자기 원인 예상값에 근접했고, 규모
미달로 기각했던 **「지각 벡터 성분 누락」 가설이 유의해졌다**(p=0.046, 복원 벡터 예측
7.95′ vs 잔차 20.73′ 로 약 38%). 잡음이 걷히자 실제 신호가 드러난 것이다.

⚠️ **대가** — 부안이 성과표에 2023 한 건뿐이라 측점이 **17 → 16** 으로 줄어 서해안
공간 커버리지를 잃는다. 복각도 악화됐다. 「conservative」(가야·거제까지 제외)는 D 가
0.5433° 로 조금 더 좋아지나 F 가 63.87 nT 로 악화되어 채택하지 않았다.

⚠️ **`lmm_verify2019.collect_raw()` 확장 주의** — 감사를 위해 지리원 야장 전체를 훑도록
바꿨는데, `load_fieldbook_2019()` 가 이 함수를 쓰므로 **모델 입력이 조용히 바뀐다**
(성과표와 같은 (측점,연도)가 중복 투입). `load_fieldbook_2019()` 안에서 **2019 분만
남기도록 명시적으로 걸러 둔다.**

**이 감사가 뒷받침하는 것**

1. 측정 절차·계산은 문제가 없다(① 완전 일치, ② 정밀도 양호). 사람이나 장비 탓이 아니다.
2. 문제는 **방문마다 달라지는 방위 기준**이다.
3. 따라서 신규 선점에서 **방위표지를 고정하고 방위각 결정법을 상향**(천문·자이로 또는
   RTK 장기선)해야 한다는 근거가 선다 — 14절의 「방위표지 100m = 방위각 정밀도 대리지표」와
   같은 결론에 독립적으로 도달한다.
4. Regional 입력 선별 기준도 이 표로 정할 수 있다 — 정상 구간 측점을 우선 투입하고,
   불일치 측점은 재측정 전까지 보류한다.

---

### 시기간 대조 — 신규 검증 지표

2019(검증완료) vs 2022~25(미검증) 겹치는 3측점의 IGRF 잔차 차(영년변화 제거됨,
0 이어야 정상):

| 측점 | 편각 차 | 총자력 차 |
|---|---|---|
| 미원 | −2.37′ | +37.3 nT |
| 이원 | −9.05′ | −82.8 nT |
| 거제 | −13.50′ | +52.8 nT |

**성과표 오류인지 외부장 미보정인지 대조만으로는 못 가린다** — 차이가 일변동 규모와
정확히 겹친다. 그러나 이것이 **External 층의 정량 검증 표적**이 된다: External 적용 후
0 으로 수렴하면 외부장, 남으면 성과표 오류 확정.

⚠️ **남지·장흥은 성과표에 없어 시기간 대조가 불가능**한데 남지 잔차가 +44.5′ 로 둘째로
크다. 검증 수단 없는 측점이 최악 잔차를 갖고 있다.

### 2019 단독 구축은 불가

5측점 / 8세션(성산 시각 미기입 제외). 최북단 미원(36.73°N) 위로 **208 km 공백**,
1차 다항식 자유도 2, `robust_lstsq` 최소 표본 6개 미달로 **이상치 배제가 작동하지 않는다**.
5개 중 남지·거제 2개가 최악 잔차 측점. 다만 **NOC 공간투영 구현의 회귀 가드**로는 유효.

### 작업 순서 (신호대잡음 근거)

CYG 로 제거 가능한 일변동은 편각 6.7′ 인데 미설명 잔차가 35′ 다. **잡음이 신호의 5배인
상태에서 보정을 얹으면 개선 여부 자체를 판정할 수 없다** (실제로 균일 V 근사는 LOO D 를
0.751°→0.858° 악화시켰다).

```
0. 2022~25 야장 확보                          ← 1·2단계 공통 전제, 최우선
   ├ 병렬 A: NOC 공간투영 구현 (2019 세션으로 회귀 가드)
   └ 병렬 B: 3측점 시기간 대조를 검증 지표로 고정 (즉시 가능)
1. 성과표 원본 감사 (lmm_verify2019 방식 확장) — 판정기준 사전 확정
2. External 포함 **end-to-end 재적합** (층이 결합되어 있어 순차 튜닝 금지)
3. 교차검증 + 독립 표본 검증
```

1단계 판정기준: 감사 후 편각 잔차 **10′ 이하** → CYG 진행 / **10~20′** → 잔차 큰 측점
현장 재측정 / **20′ 이상** → 모형 작업 중단, 방위 기준 재결정 선행.
**현재 13.75′ — 두 번째 구간**(착수 시점 35.0′ → 감사 후 20.73′ → 지각 벡터 후 13.75′).
즉 모형 작업을 계속하되, **잔차가 큰 측점의 현장 재측정**이 남은 조건이다.

⚠️ 0~2단계는 완료됐다(야장 212세션 · 감사 · end-to-end 재적합). 남은 3단계는
독립 표본 검증이며, 그 전에 위 「다음 단계」의 2·3 이 선행돼야 한다.

⚠️ **IGRF 대조는 정확도 검증이 아니다.** IGRF 는 LMM 의 입력이므로 LMM−IGRF 는
정의상 보정층의 합이다(실측 확인: −151.65 nT = Regional −3.89 + Crustal −147.76).
정당한 용도는 ① Core 구현 검증 하나이고 이미 통과했다. 정확도 검증은 LOO·hold-out·
신규 캠페인 같은 **독립 표본**이어야 한다.

---

## 14. 현장 조사서 취합 · 선점 검토 지도

각 본부가 회신한 「자기교란 현황 조사 카드」(make_survey_card_v2.py 로 배포한 양식을
채운 워크북)를 취합하고, 선점 가능성을 검토해 지도 마커로 웹에 표출한다.

```
① python aggregate_survey_xlsx.py   # 조사서 xlsx 전체 → 취합·검토 + 일괄취합 xlsx (docs/output/)
② python make_survey_map.py         # 취합 결과 → docs/survey_review.html (Folium 마커) + 사진 추출
③ python merge_survey_workbooks.py  # 회신본 → 배포 양식(총괄+카드103) 그대로 병합 (대용량)
④ python make_sketches.py                     # 기준점+방위표지1·2 → 위성배경 약도 PNG
⑤ python merge_survey_workbooks.py --augment  # 취합 엑셀에 결과 열·약도 삽입(원본 미접근)
```

⚠️ **입력 폴더명 주의** — 회신 폴더는 자주 이름이 바뀐다(`보사서→조사서`, 하위
`2.조사서_부산울산→조사서`). 그래서 경로 하드코딩 대신 **베이스 폴더 `20260811_사전 현장
조사서 취합` 아래에서 `*.조사서_*.xlsx` 를 재귀 탐색**한다(`aggregate_survey_xlsx.survey_files()`,
`~$` 임시파일 제외). `make_sketches`·`merge_survey_workbooks` 모두 이걸 재사용한다.

**약도(⑥) 생성·삽입** — 카드 ③의 기준점·방위표지1·2 좌표(본부마다 형식 상이:
`대시`/`°'\"`/`공백` → `dms2dd()` 가 숫자 3개 추출로 통일)로 위성배경(Esri z18) 약도를
그린다(`make_sketches.py`, 방위각·거리 라벨·북 화살표·축척). 좌표 완비 93/103 생성.
`--augment` 는 **원본 회신본을 다시 읽지 않고** 이미 만든 취합 엑셀을 열어 ⑥약도 칸에
약도를 넣고 총괄 목록에 결과 열(소유권·조사일·조사자·기상·종합판정·방위표지·핵심
교란요인·판정의견)을 추가한다(전체 병합 `merge_survey_workbooks.py` 는 카드 재구성까지
한 번에 하므로 카드 내용 변경 시 이걸 쓴다). openpyxl 3.1.5 는 **이미지 라운드트립 보존**
(load→save 시 사진 유지 확인). 산출 `*_취합_총괄_약도포함_전체103건.xlsx` ≈ 286 MB, `.gitignore` 제외.

**survey_review.html 토글** — `make_survey_map.py` 가 등급 마커 외에 ① 「🗺️ 도엽 경계
(1:50,000)」(`topo_sheets.geojson` 203폴리곤, index.html 동일 스타일) ② 「⭐ 기존 측정점」
(`existing_sites.geojson` 중 `EXISTING_USE` 선별, ⭐마커) 토글을 추가한다(둘 다 기본 꺼짐).
`EXISTING_USE` 는 현황표 1차 비고 초록 ∪ 2차 비고 파랑 = 24점(현재 20 확정, 개명 별칭
`EXISTING_ALIAS` 경주=영천·임계=삼척). ⚠️ **약도 배경 실패 시 저장 금지**(빈 약도 방지) —
`make_sketches.py` 는 basemap 실패하면 PNG 를 남기지 않고 다음 실행에서 재시도한다.

`merge_survey_workbooks.py` 는 11개 회신본에 흩어진 카드 103장을 **원본 배포 양식과 동일
구조**(총괄 목록 + 카드시트, DS 번호순)로 한 파일에 합친다 — 값·수식·서식·병합셀·행높이·
열너비·**임베드 사진·드롭다운·하이퍼링크**까지 셀 단위 충실 복사(openpyxl 은 워크북 간 시트
복사를 지원하지 않음). 산출 `*_현장조사_취합_총괄_전체103건.xlsx` 는 **사진 포함 ~240 MB** 라
`.gitignore` 로 제외(로컬 보관). 총괄 목록은 템플릿 원본을 복사하고 카드만 회신본에서 가져온다.

`aggregate_survey_xlsx.py` 는 **두 파일**을 낸다: `*_현장조사_취합검토_*.xlsx`(3시트, 등급
검토 포함)와 `*_현장조사_일괄취합_*.xlsx`(단일 시트 플랫 덤프 — 자기교란 존재/이격 분리,
방위표지 1·2 좌표·방위각·거리까지 전부). `make_survey_map.py` 는 카드 임베드 사진을
`docs/survey_photos/{DS}_{중심|동|서|남|북}.jpg` 로 다운스케일(≤300px) 저장하고 팝업에
썸네일(클릭 확대)과 방위표지 좌표를 넣는다.

- 입력: `20260811_사전 현장 보사서 취합/2.조사서_부산울산/` 폴더의 `*.xlsx` 11개
  (폴더명은 부산울산이나 **11개 본부 103개 카드 전부** 들어있음). 각 파일 = 총괄 목록 +
  카드 시트들. 카드 레이아웃이 고정이라 **셀 위치(라벨 앵커)** 로 안정 파싱.
- `aggregate_survey_xlsx.py` 산출 3시트: **검토 요약**(대시보드) · **선점 검토**(웹 표출용
  등급 A/B/C/D) · **현장조사 취합**(원자료 1행/후보지).
- `make_survey_map.py` 는 `aggregate_survey_xlsx` 의 `parse_workbook`·`review` 를 재사용해
  103점을 등급별 CircleMarker 로 그린다. **index.html 불변**, `/survey_review.html` 독립 배포.

| 유의점 | 내용 |
|---|---|
| **조사일 라벨은 D열** | 카드에서 `조사일` 라벨은 D11, 값은 E11. A열 맵에서 찾으면 전건 누락된다. `label_rows(ws,4)` (D맵) 사용 |
| **자동판정 재계산** | F열 판정·집계·종합판정은 수식. 파일이 Excel 로 평가 안 됐으면 캐시가 None → 존재여부(D22:D28,D29)에서 규칙으로 **재계산**한다(없음→적합, 1~2 부적합→조건부, 3+→부적합, 미입력→조사 미완료) |
| **4단계 등급 체계** | `review()` 는 **A/B/C/D** 4단계로 분류. **A 선점 가능**(적합+방위표지 좌표거리 하나라도 ≥100m) · **B 조건부 선점 가능**(적합이나 방위표지 최장 <100m — 자기 청정이므로 정밀 방위각(천문·자이로·RTK) 또는 ≥100m 표지 확보 시 선점 가능) · **C 현장 확인 필요**(조건부 적합) · **D 부적합**(부적합 또는 방위표지 불가). 색: A녹·B청(#2E86C1)·C황·D적. `aggregate_survey_xlsx.review()` 단일 소스라 지도·엑셀 동시 반영. B 는 자기적으로 양호한 점을 버리지 않으려는 구제 등급 — 새 측점 탐색보다 방위 기준 정밀화가 저비용이라는 근거 |
| **도엽은 카드 기재값이 아니라 좌표로 판정** | ⚠ 배포 카드의 도엽번호·도엽명은 **연계 기존점의 도엽**이 찍혀 있었다 — `make_survey_card_v2.py` 가 `sheet_of()` 에 후보지가 아닌 **기존점 좌표**(`exi['lon'], exi['lat']`)를 넘긴 버그. 103건 중 **79건이 오기재**(예: DS-042 영양 신규점6 → 카드엔 '청송'). 생성기는 고쳤으나 회신본은 이미 그 값으로 굳었으므로 취합 단계에서 `fix_sheet()` 가 **후보지 좌표로 재판정해 덮어쓴다**(기재값은 `도엽번호기재`·`도엽명기재`, 변경 이력은 `도엽정정` 에 보존 → 엑셀 「도엽 정정」열). 도엽 폴리곤은 전부 경위선 직교 직사각형(15'×15')이라 shapely 없이 **bbox 판정**으로 충분(`sheet_at(lat, lon)`). ⚠ 이 정정으로 **도엽 그룹이 바뀌어 B 대표/예비가 15/20 → 28/7 로 재편**된다 — 오기재가 서로 다른 후보지를 같은 기존점 도엽으로 뭉쳐 중복을 부풀렸기 때문 |
| **B 는 도엽 대표/예비로 분리해 센다** | 자침편각 표기는 **도엽당 1점**이면 충분하므로 B 35건을 전부 유효 후보로 세면 과대계상. `sheet_priority(recs)` → `{관리번호:(구분,순위,도엽내B수,사유)}` 로 **대표 28 / 예비 7**(도엽 중복 후순위 4 + 도엽에 A 이미 확보 3) 분리. **대표** = 그 도엽에 A 가 없고 도엽 내 방위표지 최장거리 1순위(★, A 승격 1순위). ⚠️ **A 보유 도엽 체크가 핵심** — 예산·임계·진안 3도엽은 이미 A 가 있어 그 안의 B 3건이 통째로 예비다(순위만 보면 놓친다). 지도는 `LAYERS` 로 「B 도엽 대표」(청 #2E86C1·흰테두리 강조)와 「B 예비」(연청 #AFCBE3) **별도 토글**, 엑셀은 「도엽 구분」열(대표 먼저 정렬)·검토 요약에 대표/예비 내역. 도엽 식별은 도엽번호 우선, 없으면 도엽명 |
| **방위표지 100m = 방위각 정밀도 대리지표** | 100m 는 거리 자체가 목적이 아니라 방위각 기준 정밀도(=좌표오차 σ/거리 L)의 대리치. 휴대GPS(σ=1m)면 100m 에서 34′(법정 §20 30′ 수준), 20m 면 172′로 열악. **RTK(σ=0.1m)면 50m 에서 6.9′(KPI 6′ 수준)**, **천문·자이로 방위각이면 거리 무관**(1등 지자기점 §19 표준). → B 승격 경로는 표지 이설(≥100m)만이 아니라 방위각 결정법 상향 |
| **방위표지 불가 = 등급 D** | 지자기점은 방위표지 필수. 종합판정이 적합/조건부여도 방위표지 '불가'면 등급 D(부적합·대체 검토)로 분류 |
| **거리는 카드값 아닌 좌표로 계산** | 카드 기재 '거리' 는 **오기입 사례**가 있다(경남 DS-004·045·084 은 좌표상 실제 20~70m 를 100~117m 로 적음 — 045·084 은 방위각이 좌표와 일치해 거리 숫자만 4~5배 부풀렸고, 004 는 방위각도 역방위다). `mark_max_dist()` 는 **기준점·표지 좌표로 계산한 측지거리(`_geo_dist`)를 우선**하고 좌표 없는 표지만 카드값 폴백. 이 정정으로 경남 3건이 A→B |
| **약도 라벨도 좌표 기준** | 삼각형은 좌표로 찍히므로 라벨이 카드값이면 그림과 모순된다. `make_sketches.py` 의 `dist_label()`·`az_label()` 이 **좌표 계산값을 라벨에 쓰고, 카드값이 어긋날 때만 괄호로 병기**한다(거리 5m·10% 초과, 방위각 5° 초과). 카드값을 지우지 않는 것은 측정 방위각이 휴대GPS 좌표보다 정확할 수 있기 때문. 불일치 건은 테두리를 적색(#CC3333)으로 바꾸고 실행 끝에 `FLAGS` 목록으로 요약한다 |
| **방위각 역방위 14쌍 — 취합에서 정정** | 방위각 규약은 **북=0°·시계방향**(§19 진북 기준, `az_from_ll()` = `atan2(동편차, 북편차)`). 카드 196쌍 중 **29쌍이 5° 초과 불일치 = 역방위(±180°) 14 + 기타 15**. 원인은 **표지→기준점 방향을 적은 역방위 기재**로 확정 — 좌표 맞바꿈이라면 표지 좌표가 후보지 자리에 와야(d_표지≈0) 하는데 최소가 22m 이고, DS-004 는 오히려 **기준점이 후보지와 0m** 로 일치한다. 즉 **좌표는 옳고 카드 숫자만 뒤집혔다**. ⚠ DS-030·088 은 기준점이 후보지에서 232·245m 떨어져(현장 이설) 이 검정이 무력하니 맞바꿈 근거로 읽지 말 것 |
| **`fix_azimuth()` — 방위각·거리 정정** | `fix_sheet()` 와 같은 방식(원본 카드 불변, 취합·검토·총괄·웹에만 반영). **5° 이내면 카드값 유지**(측정 방위각이 휴대GPS 좌표보다 정밀), **±180°(±5°) 는 카드값 +180°** 로 뒤집어 측정 정밀도를 살리고, **그 밖의 불일치는 복원할 원값이 없으므로 좌표 방위각으로 대체**한다. 거리는 항상 기준점↔표지 측지거리. 기재값은 `방위각기재`·`거리기재`, 이력은 `방위정정`(엑셀 「방위 정정」열·웹 팝업 ✔). 24건 카드에서 29쌍 정정 |
| **역방위는 조사자별로 갈린다** | 조사자 단위로 **일관**된 3명(대구경북 김상민 4/4 · 경남 양동원 4/4 · 광주전남 김진환 2/2 = 10쌍)은 **습관적 역방위 기재**라 +180° 교정이 안전하다. 나머지(윤병선·오승훈·권홍순)는 같은 조사자가 정·역을 혼용한 단발 오기입. 카드 내부에서 표지1·2 규약이 다른 4건(DS-047·054·074·080)도 오기입. ⚠ 별건으로 **대전세종충남 윤병선은 16쌍 중 9쌍이 '기타 불일치'**(±180° 아님) — 규칙성이 없어 역방위보다 심각하다. 좌표로 대체해 두었으나 **현장 재확인 1순위** |
| **후보지↔기준점 이격은 오류가 아니다** | 후보지 좌표는 **현장조사 전 도상에서 고른 점**이고, 성과의 기준은 **현장 기준점**이다. 거리·방위각은 전부 기준점↔방위표지로 재며, 이격이 크다는 것은 **도상 후보지로 진입이 어려워 옮겼다**는 접근성 지표다. `d["후보지이격"]`(엑셀 「후보지↔기준점(m)」·웹 팝업 50m 이상 표시). 분포는 중앙 17m·74/98건이 50m 미만이고 200m 초과는 11건뿐. ⚠ 그래서 `JUDGMENT_OVERRIDE` 의 **DS-011(2,049m 이격만이 사유)을 해제**했다 — 카드 원판정 조건부 적합 → 등급 C(D 14→13·C 48→49). 나머지 4건은 통신 음영·상공장애·방위표지 불가로 이격과 무관한 독립 사유라 유지 |
| **카드 거리 ~100m 는 측정값이 아닐 소지** | 거리 불일치 17쌍의 카드값이 **100~117m 에 몰려 있다**(DS-001·004·005·022·045·084·087·090·091 — 좌표상 실제는 18~75m). 측정값이 아니라 **이격 기준 100m 를 충족한다는 뜻으로 적은 것**으로 보인다. `mark_max_dist()` 가 이미 좌표를 우선하므로 등급에는 반영돼 있다(A 9→6). ⚠ DS-047 화천은 **표지1·2 거리가 서로 뒤바뀜**(카드 132/77 ↔ 좌표 77/132) — 최장값은 같아 등급 불변 |
| **PDF 회신본 파서** | 초기엔 PDF 로 회신돼 `aggregate_survey_reports.py`(pymupdf 좌표 파싱)를 만들었으나, 이후 **엑셀 카드로 회신**되어 `aggregate_survey_xlsx.py` 가 정식 경로. PDF 파서는 참고용 |
| **prefer_canvas 마커** | Folium `prefer_canvas=True` 면 CircleMarker 가 캔버스 렌더 → DOM `path` 쿼리로 안 잡힌다. 검증은 `map.eachLayer` 로 카운트 |
| **사진 앵커 분류** | 카드 임베드 이미지는 anchor (row,col) 0-index 로 슬롯 판별. **col≥7 은 지도**(I/K 열), col<7 이 사진(중심 row44·동/서 row47·남/북 row50). `extract_photos()` 참조 |
| **③ 위도 앵커 충돌** | A맵의 '위도'는 A5(위도(십진))가 선점 → 방위표지 좌표(A35 위도)를 못 읽는다. '경도'(A34) 앵커 +offset 으로 읽을 것 |
| **약도 위성타일** | Esri WorldImagery 는 시골서 z19 미제공('Map data not yet available') → z18→17→16 폴백. contextily 는 타임아웃이 없어 끊기면 무한대기 → `socket.setdefaulttimeout(20)` |
| **좌표 형식 3종** | 방위표지 좌표가 본부마다 대시/도분초/공백 혼용 → `dms2dd()` 숫자 3개 추출. 범위검증(경도124~132·위도33~39)으로 뒤바뀜·오기입 배제 |
| **약도 anchor 검증** | openpyxl 이 쓰는 drawing XML 은 **prefix 없는 기본 네임스페이스**(`<from><col><row>` — `<xdr:>` 아님). 약도는 ⑥약도 칸(col≤1·row 51~55 0-index)에 앉는다 |
| **기존 30점 중 선점 대상은 15점** (2026-08-19 정정) | `make_survey_map.EXISTING_TARGET` — 상주·미원·부안·강화·양산·남지·거제·조도·포천·성산·여주·화천·봉평·삼척·제천. 나머지 15점(남양·춘양·청송·서산·청양·이원·와도·순창·함양·가야·영천·남해·순천·장흥·설악)은 **저수지(제방) 설치점**으로 판정돼 제외한다 — 지반 거동·수위 변동으로 표석 지속성이 확보되지 않는다. survey_review.html 은 「🎯 기존 측정점 — 선점 대상 (15)」(금색 원판 ★, **기본 켬**)과 「⭐ 기존 측정점 — 기타 (15)」(기본 끔)을 **별도 토글**로 낸다. 이름은 `existing_sites.geojson` 과 직접 일치해 `EXISTING_ALIAS` 가 비어 있다(경주=영천·언양=양산·임계=삼척 도 확정 이름 쪽이 함께 실려 있다) |
| **미완료 실사례** | DS-093 거제 신규점2 는 8항목 전부 미입력으로 회신 — 재조사 대상 |

첫 검토(103건, 2026-08): 등급 A 41 · B 51 · C 10 · 미완료 1. 최다 교란요인 자연환경(수목)
38건, 방위표지 불가 8건.

4단계 등급 적용 후(2026-08-18, 좌표 계산 거리 기준): **A 6 · B 35 · C 48 · D 14 · 미완료 0**.
- A(선점 가능) 6: 임계1·화천·예산2·진안1·무풍1·태백 — 좌표상 방위표지 최장 ≥100m.
- B(조건부 선점 가능) 35 = **도엽 대표 28 + 예비 7**(중복 후순위 4 · A 확보 도엽 3):
  종합판정 적합·자기교란 0 이나 방위표지 좌표거리 모두 <100m.
  **전부 자기 청정 지점** — 정밀 방위각 확보 또는 ≥100m 표지 이설 시 A 승격 가능(구제 대상).
  문턱 근접: DS-036 예천(97m)·DS-068 관리3(91m).
- C(현장 확인 필요) 48: 카드 종합판정이 '조건부 적합'.
- D(부적합) 14: 부적합 또는 방위표지 불가(9건 포함).

⚠ **DS-011 재판정 해제 후 최신 분포는 A 6 · B 35 · C 49 · D 13**(위 표의 C 48·D 14 는
해제 전 값). A·B 와 도엽 대표/예비(28/7)는 변동 없다.
카드 기재 거리로만 보면 A 9건이나, 경남 3건 DS-004·045·084 은 카드 거리(~100m)가 좌표
(20~70m)와 불일치하여 좌표 기준으로 B 처리.

도엽 좌표 재판정 후(2026-08-18): 등급 분포(A/B/C/D)는 **불변**이나 — `review()` 는 도엽을
쓰지 않는다 — **B 대표/예비가 15/20 → 28/7** 로 바뀐다. 도엽 오기재 79건이 후보지들을
연계 기존점 도엽(춘양·청송·상주·공주 등)으로 뭉쳐 중복을 부풀렸던 탓이다. 실제로는 대부분
**서로 다른 도엽에 단독으로 놓인 B**(예: 영양 도엽 3건·담양 2건·회천 2건만 중복).

---

### 한국 지자기도 2D — `docs/lmm_map.html` (2026-08-19)

2020년 연구 보고서의 「그림 4-21/4-22」(IGRF의 복각과 보정된 복각의 지자기도)와 **같은
형식**으로 IGRF · LMM · 차이를 3면 나란히 그린다. 발표에서 2020 성과와 직접 대비할 때 쓴다.

| 요점 | 내용 |
|---|---|
| 계산 | `igrf14.js` + `data/lmm_model.json` · 격자 150×190(약 3~4 km) · 오프라인 |
| 척도 | IGRF·LMM 은 **같은 색범위**를 쓴다(비교가 성립하려면 필수). 차이는 0 중심 발산형 |
| 색띠 | 연속 그라디언트가 아니라 **등간격 14단 계단**(보고서 지자기도와 동일) |
| 표시 | 해안선 · Regional 측점 · 경위도 축 · 컬러바 · 성분(F/D/I)·연도 전환 |

⚠️ **등치선은 마칭스퀘어로 그리지 말 것** — 이 해상도에서는 끊긴 십자 점으로 찍혀
지저분하다. **색띠 경계 화소를 어둡게**(`bandOf[k]!==bandOf[k-1]`) 하면 채색과 정확히
일치하는 1px 등치선이 나온다.

⚠️ **값 표시에는 순차형 색표** — 발산형은 중앙이 백색이라 한국 편각처럼 부호가 안 바뀌는
구간(−9.4°~−7.8°)에서 중간값이 **흰 띠**로 나와 「자료 없음」처럼 보인다. 지구본 패치에서
실제로 발생했던 증상이다. 발산형은 0 이 의미를 갖는 차이 표시에만 쓴다.

---

## 15. 3D 지구 자기장 (IGRF-14) — `docs/geomag_globe.html`

전 지구 자기장을 3D 지구본에 표출. **1단계 완료**: 자기장을 지구 표면에 색으로 입힌
"세계 자기지도" — 성분(F·D·I) 전환 + 등치선(등자력·등편각·등복각, agonic·dip-equator
강조) + 세계 해안선 + 한국 경계 + 자기력선 + 범례 + 호버 D·I·F + 「한국으로」 카메라 이동.
⚠️ 사용자 제공 예시(degree-1 쌍극자·파란 구+선)를 **그대로 답습하지 말 것** — 표면 색지도
중심의 다른 디자인.

**2단계 완료 (2026-08-19) — 한국 LMM 고해상 패치** 「🇰🇷 한국 LMM」 토글.
전역 격자는 2°(IGRF)로 두고 **한국 구간만 0.05° 로 다시 계산**해 지표 위(r=1.004)에
얹는다. 전역 해상도를 올리면 계산량이 해상도²로 늘지만 국지 패치는 한국 면적만 계산한다.

| 요점 | 내용 |
|---|---|
| 결합식 | `lmm.html` 과 동일 — D=IGRF_D+reg_D · I=IGRF_I+reg_I · F=IGRF_F+crustal+reg_F. External 미적용 |
| 격자 | 33.0~38.7°N · 125.0~130.0°E · 0.05° → 101×115 = **11,615 정점** |
| 검증 | Python 재현과 **D 0.07″ · I 0.06″ · F 0.01 nT** 일치 (36.5°N 127.5°E, 2026.0) |
| 커서 | 패치 안에서는 커서 값도 LMM 으로 읽는다(`LMM` 배지 표시). 밖은 IGRF |
| 상태 표시 | 켜면 LOO·측점수와 **「연구·시범계산용, 정식 편각 산출 금지」** 경고를 함께 띄운다 |
| 점검 훅 | 모듈 스코프라 콘솔에서 안 보인다 → `window.__lmm()` 로 정점수·샘플값 확인 |

⚠️ **정점색은 작업 색공간(선형)으로 넣어야 한다** — `colorAt()` 이 주는 값은 sRGB(0~255)라
그대로 `BufferAttribute` 에 넣으면 패치만 밝게 떠서 전역 표면과 톤이 어긋난다.
`_c.setRGB(r,g,b,THREE.SRGBColorSpace)` 로 변환해 넣을 것.

⚠️ **메시는 DoubleSide** — FrontSide 면 카메라 각도에 따라 뒷면이 컬링돼 사라진다.

⚠️ **패치에는 자기 색막대를 붙일 것** — 패치는 전역 표면과 **색표도 범위도 다르다**
(순차형 CM_F · 자기 분포 2~98 백분위). 화면의 범례는 전역 것(발산형 −25~+25°)이라,
서편각뿐인 한국 편각 패치가 **「빨강 = 동편각」으로 오독된다.** 패널에 패치 전용
그라디언트 막대(`lmmGradient()`)와 최소·중앙·최대 라벨을 넣고, 패치가 켜지면 전역
범례 제목에 「— 전역 표면 기준」 꼬리표를 붙인다.

⚠️ **패치는 전역 색범위를 쓰면 안 된다** — F 전역 범위는 42,000 nT 폭인데 한국 변화폭은
약 3,700 nT 라 한 덩어리로 뭉갠다(초기 구현의 실제 증상: 밋밋한 사각형). 패치는 **자기 값
분포로 색범위를 다시 잡는다**(2~98 백분위). 전역 범례와 척도가 다르므로 안내문에 범위를
반드시 표기한다.

**두 가지 표시 방식** — 안내판에서 전환한다.

| 방식 | 색범위(예) | 보이는 것 |
|---|---|---|
| **LMM 값** | 48,459 ~ 52,141 nT | 남북 경사 + 지각 이상 얼룩 |
| **LMM − IGRF** | ±238 nT (발산형, 0 중심) | **지각 자기이상 구조 자체**. 해상은 자료가 없어 0(백색) |

「LMM−IGRF」가 과학적으로 가장 선명하다 — 항공자력 자료 공백(해상)이 백색으로 드러나
자료 범위를 눈으로 확인할 수 있다.

⚠️ **패치도 전역 표면과 같은 음영식을 써야 한다** — `MeshBasicMaterial`(무광)이면 패치만
밝게 떠 보인다. 지구 셰이더와 동일한 `col*(0.62+0.5*sh)`, `sh=0.35+0.65*max(dot(n,L),0)` 을
적용한 `ShaderMaterial` 로 칠한다.

**3단계 완료 — 선점/기존점 레이어**: 「선점 후보」(103, 등급 A/B/C 색 구 마커) · 「기존점」
(24, 금색 팔면체) 토글. 켜면 자동으로 한국 확대(`flyKorea`), 마커 클릭 시 raycast → 정보
팝업(`#siteinfo`: 등급·판정·방위표지·교란·조사자 / 기존점은 D·I·F·설치연도). 데이터는
`export_globe_sites.py` 가 조사서·existing_sites 에서 `docs/data/survey_sites.geojson`(103)·
`existing24.geojson`(24) 로 생성(정적, Pages fetch). 마커는 실좌표 `sph()` 배치라 지도
해상도와 무관하게 정확. ⚠️ **click 핸들러의 `ray`·`mouse` 는 hover 섹션에서 선언** —
콜백은 클릭 시(모듈 평가 후) 실행되므로 TDZ 문제 없음.

⚠️ **마커 크기는 카메라 거리 비례 스케일** — 고정 월드 크기면 확대 시 거대한 덩어리가 됨
(`m.scale.setScalar(camDist*0.5)` 로 화면상 일정 작은 점 유지). 밀집 검토는 상세지도에서.

**구글어스식 드릴다운** — 한국 깊이 확대(camDist<1.48, 마커 토글 켠 상태)하거나 「🛰 상세」
버튼 → `survey_review.html`(Esri 위성 + 등급 마커 + 팝업)을 **iframe 오버레이**(`#detailwrap`,
`openDetail`/`closeDetail`)로 띄운다. 「◂ 지구본으로」로 닫으면 카메라가 물러나(×2.9) 재트리거
방지. iframe src 는 최초 열 때만 설정(지연 로드).

**LLM 질의판 (로컬 서버 전용)** — `docs/geomag_globe_llm.html` (globe 복사 + 좌측 LUI 패널).
`sv_server.py` 의 `POST /api/chat` → `globe_agent.answer()` 가 처리. 2026_ex_simul LUI 패턴을
따라 **소형 로컬 모델의 tool-calling 을 피하고**, 서버가 데이터·수치를 계산해 컨텍스트로 주입
→ LLM 은 서술만. ① IGRF 계산(ppigrf, 지명 게이지티어+영년변화) ③ 선점 검색(survey_sites/
existing24). 반환 `{answer, actions, tier}`, actions=focus/highlight/focus_korea 로 지구본 조종.
LM Studio(`localhost:1234/v1`, exaone-3.5-2.4b, `openai` SDK, 키 불필요) 미실행이면 서버 계산
결과만(tier=data-only, `max_retries=0` 로 즉시 폴백). 실행: `python sv_server.py` + LM Studio.

**LLM 4-티어 폴백** — `GLOBE_LLM_BACKEND=auto`(기본) 면 LM Studio → **Gemini API**(`.env` 의
`GEMINI_API_KEY` 있을 때) → **OpenAI API**(`OPENAI_API_KEY` 있을 때) → 서버계산.
`gemini`/`openai`/`lmstudio` 로 고정도 가능. `.env`(루트, `.gitignore`
제외)를 **python-dotenv 없이 간이 파서**(`_load_env`)로 읽는다(설정 예시 `.env.example`). Gemini
는 OpenAI 호환 엔드포인트(`https://generativelanguage.googleapis.com/v1beta/openai/`)를 제공하므로
`openai` 패키지를 그대로 재사용(`globe_agent._gemini`, base_url 만 교체). 두 키 모두 **서버(.env)에만**
두고 브라우저엔 노출 안 됨. tier: local🟢 / gemini🟣 / openai🔵 / data-only🟡.
⚠ **OpenAI 크레딧 소진 시 429 `insufficient_quota`** — `GEMINI_API_KEY` 로 즉시 무료 폴백 가능
(Gemini 는 무료 티어 제공). `openai` 패키지 예외 메시지에 사유가 그대로 담기므로 `err` 필드로 확인.
⚠ **Pages 배포엔 부적합**(/api/chat 서버 필요) — Pages 판 `geomag_globe.html` 은 LUI 없이
깔끔한 지구본(F/D/I·등치선·자기력선·선점/기존 마커·드릴다운)만 유지. (브라우저 규칙기반 LUI 를
`geomag_globe.html` 에 붙였으나 사용자 판단으로 되돌림 — 커밋 `0bb40ee` 참고, `geomag_globe_llm.html`
로컬판만 LUI 유지.)

```
① LM Studio 실행(모델 로드) → localhost:1234
② python sv_server.py            # docs 서빙 + /api/chat
③ http://127.0.0.1:8899/geomag_globe_llm.html
```

**해상도 주의(3단계 대비)** — 전역 F/D/I 격자는 2°(91×181≈16k igrf 호출/재계산). 올리면
계산량이 **해상도²** 로 증가(1°≈4×·0.5°≈16×, 연도 변경마다 재계산). 표면은 `LinearFilter`
쌍선형 보간이라 2°도 매끈하고, **선점/기존점 마커는 실좌표 `sph()` 배치라 지도 해상도와
무관하게 정확**하다. 한국만 고해상이 필요하면 전역 격자를 올리지 말고 **국지 패치**로 할 것.

**LMM 재추가 시 참고(보류 중)** — `lmm.html` 의 `lmm()` 결합식: D=IGRF_D+reg_D,
I=IGRF_I+reg_I, F=IGRF_F+crustal(F만)+reg_F, External=null. regional 은 degree-1 다항식
(u=lat−36, v=lon−127.5). crustal 154×198 2.8km(`values[j*nlon+i]`, i=경도 서→동, j=위도
남→북). 패치 메시는 **DoubleSide**(FrontSide 면 뒷면 컬링). 카메라 이동은 `camGoal` +
animate 의 `camera.position.lerp(camGoal, 1-0.0025^dt)`, `minDistance` 1.25.

```
docs/igrf14.js               # IGRF-14 구면조화 엔진(재사용 모듈) — window.IGRF
docs/geomag_globe.html       # Three.js 세계 자기지도 3D (CDN three, Pages 배포)
docs/data/world_coastline.geojson  # Natural Earth 110m 해안선(134선, 로컬 저장)
docs/data/korea_boundary.geojson   # root data/ 에서 복사(fetch 는 docs 기준 상대경로)
```

- **`igrf14.js`** 는 `lmm.html` 의 검증된 IGRF 엔진(`function igrf`·legendre·interpCoef)과
  계수 `DATA.igrf`(nmax13·4에폭 2015~2030)를 **추출**해 IIFE 로 감싼 것. `window.IGRF =
  {igrf(lat,lon,elevM,year)→{X,Y,Z}, igrfElements→{X,Y,Z,H,F,D,I}, IGRF14}`.
  **ppigrf 와 완전 일치 검증**(서울 D −8.983°·I 54.553°·F 51,372nT, 남대서양이상 F 22,842nT).
- **globe.html**: 예시(사용자 제공 degree-1 쌍극자)를 **degree-13 실측 필드**로 교체.
  `Bvec(P,year)` 가 씬 좌표 P→(lat,lon,elev)→igrf X/Y/Z→국소 북·동·하 기저로 3D 벡터 합성,
  RK4 로 자기력선 추적. 세기색(F 램프)·입자흐름·대기 글로우·UnrealBloom·자전·연도 슬라이더.
- 좌표 규약: Y축=북극, `rhat=(cosφcosλ, sinφ, cosφsinλ)`. lmm.html/예시와 동일.

| 유의점 | 내용 |
|---|---|
| **CDN 필요** | three@0.180 + addons(EffectComposer·UnrealBloomPass)를 importmap 으로 CDN 로드 → **Artifact CSP 불가, Pages 로만 배포**. igrf14.js·geojson 은 로컬 fetch |
| **패널 미표시 시 렌더 0** | 미리보기 패널이 화면에 없으면 innerWidth=0 → 캔버스 0×0 (기능은 정상). 실제 브라우저/Pages 에서 렌더 |
| **필드 추적 성능** | igrf(nmax13)×RK4 4회×~800스텝×라인수 → rebuild 는 디바운스(연도 변경 시만). 모바일 라인수/픽셀비 축소 |
| **엔진 동기화** | igrf14.js 는 lmm.html 엔진의 **사본**. lmm.html IGRF 를 고치면 igrf14.js 도 재추출할 것 |
| **초기 계산은 setTimeout** | 표면 색지도·등치선 계산을 `requestAnimationFrame` 으로 지연하면 **패널/탭 숨김 시 rAF 멈춰** 계산 안 됨. `setTimeout`(숨겨도 실행)으로 할 것 |
| **표면 색지도 정합** | Earth 셰이더가 지오메트리 UV 대신 **오브젝트 좌표 normal→lat/lon**(`lat=asin(n.y), lon=atan2(n.z,n.x)`)로 equirect 텍스처 샘플 → 호버·해안선·자기력선과 좌표계 일치. 자전은 earthGroup 전체를 돌려 지리계 동반 |
| **경계 fetch 경로** | geojson 은 `docs/` 기준 상대경로(`data/...`)로 fetch. root `data/` 파일은 `docs/data/` 로 복사해야 Pages 에서 로드됨 |
