# ── 중간 산출물 (build 결과, git 제외) ───────────────────────
/output/

# ── 프레젠테이션 ──────────────────────────────────────────────
pptx/

# ── 로컬 전용 실행 스크립트 (PC별 경로 포함, 커밋 안 함) ──────
run_viewer.bat

# ── 대용량 데이터 원본 ────────────────────────────────────────
data/zone_cache/
# CYG 1분 자료 캐시 (HAPI 에서 재다운로드 가능)
data/cyg/
# KASA 관측소 자료 캐시 (kasa_geomag.py 로 재다운로드 가능, README_coverage.md 는 커밋)
data/kasa/*.csv
data/*.zip
data/*.hwp
data/*.pdf
data/*.xls
data/*.xlsx
data/*.html
data/*.dat
data/*.csv
data/*.json
data/수치지질도_25만축척_전국/

# ── docs 내 잘못 생성되는 폴더 ───────────────────────────────
docs/output/data/

# ── 대용량 조사카드 엑셀 (지도 이미지 포함, 로컬 보관) ───────
docs/output/*_도상선점_조사카드_*.xlsx
# 병합 취합 워크북(103카드+사진, 수백 MB) — 로컬 보관, 커밋 안 함
docs/output/*_현장조사_취합_총괄_*.xlsx

# ── Claude 내부 폴더 ─────────────────────────────────────────
.claude/

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# ── OS / IDE ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Office 임시 파일 ──────────────────────────────────────────
~$*
